# generated by fastapi-codegen:
#   filename:  openapi.yaml
#   timestamp: 2025-06-28T19:06:18+00:00



import argparse
import json
import os
from datetime import date as date_aliased
from typing import *
from typing import Optional, Union

from autogen.mcp.mcp_proxy import MCPProxy
from autogen.mcp.mcp_proxy.security import APIKeyHeader, APIKeyQuery, BaseSecurity

from models import (
    AfName,
    AggregateBy,
    AmendmentIndicator,
    AmendmentIndicator1,
    AmendmentIndicator2,
    AmendmentIndicator3,
    AmendmentIndicator4,
    AnalystId,
    AnalystShortId,
    AoCategory,
    AoEntityName,
    AoName,
    AoNo,
    AoRegulatoryCitation,
    AoRequestorType,
    AoStatutoryCitation,
    AuditCandidateSearchList,
    AuditCaseId,
    AuditCasePage,
    AuditCategoryPage,
    AuditCommitteeSearchList,
    AuditId,
    AuditPrimaryCategoryPage,
    BaseF3FilingPage,
    BaseF3PFilingPage,
    BaseF3XFilingPage,
    BeginningImageNumber,
    CalendarCategoryId,
    CalendarDatePage,
    CandidateCommitteeId,
    CandidateDetailPage,
    CandidateHistoryPage,
    CandidateHistoryTotalPage,
    CandidateId,
    CandidateName,
    CandidateOffice,
    CandidateOffice1,
    CandidateOfficeDistrict,
    CandidateOfficeState,
    CandidatePage,
    CandidateParty,
    CandidateSearch1,
    CandidateSearchList,
    CandidateStatus,
    CandidateStatus1,
    CandidateStatus2,
    CandidateStatus3,
    CandidateTotalAggregatePage,
    CaseDispositions,
    CaseDocCategoryId,
    CaseNo,
    CaseRegulatoryCitation,
    CaseStatutoryCitation,
    CCTotalsByCandidatePage,
    CommitteeDesignation,
    CommitteeDetailPage,
    CommitteeHistoryProfilePage,
    CommitteeId,
    CommitteePage,
    CommitteeReportsPage,
    CommitteeSearchList,
    CommitteeState,
    CommitteeTotalsPage,
    CommitteeType,
    CommitteeType1,
    CommitteeType2,
    CommitteeType3,
    CommitteeType4,
    CommunicationCostByCandidatePage,
    CommunicationCostPage,
    ContributorCity,
    ContributorEmployer,
    ContributorId,
    ContributorName,
    ContributorOccupation,
    ContributorState,
    ContributorType,
    ContributorType1,
    ContributorZip,
    CreditorDebtorName,
    Cycle,
    Description,
    Designation,
    Designation1,
    Designation2,
    Designation3,
    Designation4,
    Designation5,
    Designation6,
    DisbursementDescription,
    DisbursementPurposeCategory,
    District,
    DocumentType,
    ECTotalsByCandidatePage,
    EFilingsPage,
    ElectionDatesGetResponse,
    ElectionDistrict,
    ElectioneeringByCandidatePage,
    ElectioneeringPage,
    ElectionPage,
    ElectionParty,
    ElectionsListPage,
    ElectionState,
    ElectionSummary,
    ElectionTypeId,
    ElectionYear,
    ElectionYear8,
    ElectionYear9,
    Email,
    Employer,
    EntityReceiptDisbursementTotalsPage,
    EntityType,
    EntityType1,
    FileNumber,
    FilerType,
    FilingForm,
    FilingFrequency,
    FilingFrequency1,
    FilingFrequency2,
    FilingFrequency3,
    FilingsPage,
    FormCategory,
    FormType,
    IETotalsByCandidatePage,
    ImageNumber,
    IncumbentChallenge,
    IncumbentChallenge1,
    IncumbentChallenge2,
    IncumbentChallenge3,
    IsNotice,
    LegalSearchGetResponse,
    LoanSourceName,
    Name,
    Occupation,
    Office,
    Office1,
    Office2,
    Office3,
    Office4,
    Office5,
    Office11,
    Office12,
    Office13,
    Office19,
    Office20,
    Office22,
    Office23,
    OfficeSought,
    OperationsLogPage,
    OrganizationType,
    OrganizationType1,
    OrganizationType2,
    OrganizationType3,
    Party,
    Party5,
    Party7,
    PayeeName,
    PresidentialByCandidatePage,
    PresidentialBySizePage,
    PresidentialByStatePage,
    PresidentialCoveragePage,
    PresidentialSummaryPage,
    PrimaryCategoryId,
    PrimaryCategoryName,
    PrimaryGeneralIndicator,
    Purpose,
    Q,
    QFiler,
    Qq,
    QSpender,
    RadAnalystPage,
    RecipientCity,
    RecipientCommitteeDesignation,
    RecipientCommitteeDesignation1,
    RecipientCommitteeId,
    RecipientCommitteeOrgType,
    RecipientCommitteeOrgType1,
    RecipientCommitteeType,
    RecipientCommitteeType1,
    RecipientId,
    RecipientName,
    RecipientState,
    Renderer,
    ReportingDatesGetResponse,
    ReportType1,
    ReportYear,
    RequestType,
    ScheduleAByEmployerPage,
    ScheduleAByOccupationPage,
    ScheduleABySizeCandidatePage,
    ScheduleABySizePage,
    ScheduleAByStateCandidatePage,
    ScheduleAByStatePage,
    ScheduleAByStateRecipientTotalsPage,
    ScheduleAByZipPage,
    ScheduleAEfilePage,
    ScheduleAPage,
    ScheduleBByPurposePage,
    ScheduleBByRecipientIDPage,
    ScheduleBByRecipientPage,
    ScheduleBEfilePage,
    ScheduleBPage,
    ScheduleEByCandidatePage,
    ScheduleEEfilePage,
    ScheduleEPage,
    SchedulesScheduleCGetResponse,
    SchedulesScheduleCSubIdGetResponse,
    SchedulesScheduleDGetResponse,
    SchedulesScheduleDSubIdGetResponse,
    SchedulesScheduleFGetResponse,
    SchedulesScheduleFSubIdGetResponse,
    Size,
    Size1,
    Sort,
    SpenderCommitteeDesignation,
    SpenderCommitteeDesignation1,
    SpenderCommitteeOrgType,
    SpenderCommitteeOrgType1,
    SpenderCommitteeType,
    SpenderCommitteeType1,
    SpenderName,
    SponsorCandidateId,
    State,
    StateElectionOfficeInfoPage,
    StatusNum,
    Summary,
    SupportOppose,
    SupportOpposeIndicator,
    SupportOpposeIndicator1,
    SupportOpposeIndicator3,
    SupportOpposeIndicator4,
    TelephoneExt,
    Title,
    TotalByOfficeByPartyPage,
    TotalByOfficePage,
    TreasurerName,
    TwoYearTransactionPeriod,
    Type,
    Year,
    Zip,
    Zip1,
)

app = MCPProxy(
    contact={'x-twitter': 'FEC'},
    description='This application programming interface (API) allows you to explore the way candidates and committees fund their campaigns. \n\n The Federal Election Commission (FEC) API is a RESTful web service supporting full-text and field-specific searches on FEC data. [Bulk downloads](https://www.fec.gov/data/advanced/?tab=bulk-data) are available on the current site. Information is tied to the underlying forms by file ID and image ID. Data is updated nightly. \n\n There are a lot of data, and a good place to start is to use search to find interesting candidates and committees. Then, you can use their IDs to find report or line item details with the other endpoints. If you are interested in individual donors, check out contributor information in the `/schedule_a/` endpoints. \n\n <b class="body" id="getting_started_head">Getting started with the openFEC API</b><br> \n\n If you would like to use the FEC\'s API programmatically, you can sign up for your own API key using our form. Alternatively, you can still try out our API without an API key by using the web interface and using DEMO_KEY. Note that when you use the openFEC API you are subject to the [Terms of Service](https://github.com/fecgov/FEC/blob/master/TERMS-OF-SERVICE.md) and [Acceptable Use policy](https://github.com/fecgov/FEC/blob/master/ACCEPTABLE-USE-POLICY.md). \n\n Signing up for an API key will enable you to place up to 1,000 calls an hour. Each call is limited to 100 results per page. You can email questions, comments or a request to get a key for 7,200 calls an hour (120 calls per minute) to <a href="mailto:APIinfo@fec.gov">APIinfo@fec.gov</a>. You can also ask questions and discuss the data in a community led [group](https://groups.google.com/forum/#!forum/fec-data). \n\n The model definitions and schema are available at [/swagger](/swagger/). This is useful for making wrappers and exploring the data. \n\n A few restrictions limit the way you can use FEC data. For example, you can’t use contributor lists for commercial purposes or to solicit donations. [Learn more here](https://www.fec.gov/updates/sale-or-use-contributor-information/). \n\n [Inspect our source code](https://github.com/fecgov/openFEC). We welcome issues and pull requests! \n\n <p><br></p> <h2 class="title" id="signup_head">Sign up for an API key</h2> <div id="apidatagov_signup">Loading signup form...</div>',
    title='OpenFEC',
    version='1.0',
    servers=[{'url': '/v1'}],
)


@app.get(
    '/audit-case/',
    description=""" 
This endpoint contains Final Audit Reports approved by the Commission since inception.
The search can be based on information about the audited committee (Name, FEC ID Number, Type, 
Election Cycle) or the issues covered in the report.
 """,
    tags=[
        'audit_case_management',
        'candidate_information_management',
        'committee_operations_management',
        'filing_records_management',
        'candidate_committee_audit',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_audit_case_(
    audit_case_id: Optional[AuditCaseId] = None,
    cycle: Optional[Cycle] = None,
    sub_category_id: Optional[str] = 'all',
    sort_nulls_last: Optional[bool] = False,
    sort_hide_null: Optional[bool] = False,
    min_election_cycle: Optional[int] = None,
    audit_id: Optional[AuditId] = None,
    q: Optional[Q] = None,
    per_page: Optional[int] = 20,
    max_election_cycle: Optional[int] = None,
    candidate_id: Optional[CandidateId] = None,
    committee_type: Optional[CommitteeType] = None,
    qq: Optional[Qq] = None,
    page: Optional[int] = 1,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    committee_designation: Optional[str] = None,
    primary_category_id: Optional[str] = 'all',
    sort_null_only: Optional[bool] = False,
    sort: Optional[Sort] = ['-cycle', 'committee_name'],
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/audit-category/',
    description=""" 
This lists the options for the categories and subcategories available in the /audit-search/ endpoint.
 """,
    tags=[
        'filing_records_management',
        'state_election_office_data_management',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_audit_category_(
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    primary_category_name: Optional[PrimaryCategoryName] = None,
    sort_hide_null: Optional[bool] = False,
    api_key: str = 'DEMO_KEY',
    primary_category_id: Optional[PrimaryCategoryId] = None,
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = 'primary_category_name',
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/audit-primary-category/',
    description=""" 
This lists the options for the primary categories available in the /audit-search/ endpoint.
 """,
    tags=[
        'filing_records_management',
        'state_election_office_data_management',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_audit_primary_category_(
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    primary_category_name: Optional[PrimaryCategoryName] = None,
    sort_hide_null: Optional[bool] = False,
    api_key: str = 'DEMO_KEY',
    primary_category_id: Optional[PrimaryCategoryId] = None,
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = 'primary_category_name',
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/calendar-dates/',
    description=""" 
Combines the election and reporting dates with Commission meetings, conferences, outreach, Advisory Opinions, rules, litigation dates and other
events into one calendar.

State and report type filtering is no longer available.
 """,
    tags=[
        'calendar_event_management',
        'state_election_office_data_management',
        'filing_records_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_calendar_dates_(
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    min_start_date: Optional[date_aliased] = None,
    calendar_category_id: Optional[CalendarCategoryId] = None,
    sort_hide_null: Optional[bool] = False,
    api_key: str = 'DEMO_KEY',
    min_end_date: Optional[date_aliased] = None,
    event_id: Optional[int] = None,
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    description: Optional[Description] = None,
    sort: Optional[str] = '-start_date',
    max_end_date: Optional[date_aliased] = None,
    summary: Optional[Summary] = None,
    max_start_date: Optional[date_aliased] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/calendar-dates/export/',
    description=""" 
Returns CSV or ICS for downloading directly into calendar applications like Google, Outlook or other applications.

Combines the election and reporting dates with Commission meetings, conferences, outreach, Advisory Opinions, rules, litigation dates and other
events into one calendar.

State filtering now applies to elections, reports and reporting periods.

Presidential pre-primary report due dates are not shown on even years.
Filers generally opt to file monthly rather than submit over 50 pre-primary election
reports. All reporting deadlines are available at /reporting-dates/ for reference.

This is [the sql function](https://github.com/fecgov/openFEC/blob/develop/data/migrations/V40__omnibus_dates.sql)
that creates the calendar.

 """,
    tags=['calendar_event_management'],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_calendar_dates_export_(
    renderer: Optional[Renderer] = 'ics',
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    min_start_date: Optional[date_aliased] = None,
    calendar_category_id: Optional[CalendarCategoryId] = None,
    sort_hide_null: Optional[bool] = False,
    api_key: str = 'DEMO_KEY',
    min_end_date: Optional[date_aliased] = None,
    event_id: Optional[int] = None,
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    description: Optional[Description] = None,
    sort: Optional[str] = '-start_date',
    max_end_date: Optional[date_aliased] = None,
    summary: Optional[Summary] = None,
    max_start_date: Optional[date_aliased] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/candidate/{candidate_id}/',
    description=""" 
This endpoint is useful for finding detailed information about a particular candidate. Use the
`candidate_id` to find the most recent information about that candidate.
 """,
    tags=[
        'candidate_information_management',
        'committee_operations_management',
        'candidate_financial_statistics',
        'filing_records_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_candidate__candidate_id_(
    office: Optional[Office] = None,
    candidate_status: Optional[CandidateStatus] = None,
    cycle: Optional[Cycle] = None,
    sort_nulls_last: Optional[bool] = False,
    state: Optional[State] = None,
    district: Optional[District] = None,
    year: Optional[str] = None,
    name: Optional[Name] = None,
    sort_hide_null: Optional[bool] = False,
    federal_funds_flag: Optional[bool] = None,
    per_page: Optional[int] = 20,
    page: Optional[int] = 1,
    sort: Optional[str] = 'name',
    has_raised_funds: Optional[bool] = None,
    election_year: Optional[ElectionYear] = None,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    incumbent_challenge: Optional[IncumbentChallenge] = None,
    party: Optional[Party] = None,
    candidate_id: str = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/candidate/{candidate_id}/committees/',
    description=""" 
This endpoint is useful for finding detailed information about a particular committee or
filer. Use the `committee_id` to find the most recent information about the committee.
 """,
    tags=[
        'candidate_information_management',
        'committee_operations_management',
        'filing_records_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_candidate__candidate_id_committees_(
    designation: Optional[Designation] = None,
    committee_type: Optional[CommitteeType1] = None,
    page: Optional[int] = 1,
    filing_frequency: Optional[FilingFrequency] = None,
    cycle: Optional[Cycle] = None,
    sort_hide_null: Optional[bool] = False,
    year: Optional[Year] = None,
    sort_nulls_last: Optional[bool] = False,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    organization_type: Optional[OrganizationType] = None,
    sort: Optional[str] = 'name',
    candidate_id: str = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/candidate/{candidate_id}/committees/history/',
    description=""" 
Explore a filer's characteristics over time. This can be particularly useful if the
committees change treasurers, designation, or `committee_type`.
 """,
    tags=[
        'candidate_information_management',
        'filing_records_management',
        'financial_totals_summary',
        'state_election_office_data_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_candidate__candidate_id_committees_history_(
    designation: Optional[Designation1] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    sort_hide_null: Optional[bool] = False,
    election_full: Optional[bool] = True,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = '-cycle',
    candidate_id: str = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/candidate/{candidate_id}/committees/history/{cycle}/',
    description=""" 
Explore a filer's characteristics over time. This can be particularly useful if the
committees change treasurers, designation, or `committee_type`.
 """,
    tags=[
        'candidate_information_management',
        'candidate_committee_audit',
        'filing_records_management',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_candidate__candidate_id_committees_history__cycle_(
    designation: Optional[Designation2] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    sort_hide_null: Optional[bool] = False,
    election_full: Optional[bool] = True,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = '-cycle',
    candidate_id: str = ...,
    cycle: int = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/candidate/{candidate_id}/filings/',
    description=""" 
All official records and reports filed by or delivered to the FEC.

Note: because the filings data includes many records, counts for large
result sets are approximate; you will want to page through the records until no records are returned.
 """,
    tags=[
        'filing_records_management',
        'electronic_filing_reports_management',
        'candidate_information_management',
        'candidate_committee_audit',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_candidate__candidate_id_filings_(
    office: Optional[Office1] = None,
    file_number: Optional[FileNumber] = None,
    form_category: Optional[FormCategory] = None,
    cycle: Optional[Cycle] = None,
    district: Optional[District] = None,
    document_type: Optional[DocumentType] = None,
    sort_hide_null: Optional[bool] = False,
    state: Optional[State] = None,
    amendment_indicator: Optional[AmendmentIndicator] = None,
    sort_nulls_last: Optional[bool] = False,
    is_amended: Optional[bool] = None,
    filer_type: Optional[FilerType] = None,
    max_receipt_date: Optional[date_aliased] = None,
    per_page: Optional[int] = 20,
    primary_general_indicator: Optional[PrimaryGeneralIndicator] = None,
    request_type: Optional[RequestType] = None,
    report_year: Optional[ReportYear] = None,
    form_type: Optional[FormType] = None,
    committee_type: Optional[str] = None,
    beginning_image_number: Optional[BeginningImageNumber] = None,
    page: Optional[int] = 1,
    sort: Optional[Sort] = ['-receipt_date'],
    api_key: str = 'DEMO_KEY',
    min_receipt_date: Optional[date_aliased] = None,
    sort_null_only: Optional[bool] = False,
    q_filer: Optional[QFiler] = None,
    report_type: Optional[ReportType1] = None,
    party: Optional[Party] = None,
    most_recent: Optional[bool] = None,
    candidate_id: str = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/candidate/{candidate_id}/history/',
    description=""" 
Find out a candidate's characteristics over time. This is particularly useful if the
candidate runs for the same office in different districts or you want to know more about a candidate's
previous races.

This information is organized by `candidate_id`, so it won't help you find a candidate
who ran for different offices over time; candidates get a new ID for each office.
 """,
    tags=[
        'candidate_information_management',
        'filing_records_management',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_candidate__candidate_id_history_(
    api_key: str = 'DEMO_KEY',
    sort_nulls_last: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort_null_only: Optional[bool] = False,
    sort_hide_null: Optional[bool] = False,
    sort: Optional[str] = '-two_year_period',
    election_full: Optional[bool] = True,
    page: Optional[int] = 1,
    candidate_id: str = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/candidate/{candidate_id}/history/{cycle}/',
    description=""" 
Find out a candidate's characteristics over time. This is particularly useful if the
candidate runs for the same office in different districts or you want to know more about a candidate's
previous races.

This information is organized by `candidate_id`, so it won't help you find a candidate
who ran for different offices over time; candidates get a new ID for each office.
 """,
    tags=[
        'candidate_information_management',
        'filing_records_management',
        'election_information_retrieval',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_candidate__candidate_id_history__cycle_(
    api_key: str = 'DEMO_KEY',
    sort_nulls_last: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort_null_only: Optional[bool] = False,
    sort_hide_null: Optional[bool] = False,
    sort: Optional[str] = '-two_year_period',
    election_full: Optional[bool] = True,
    page: Optional[int] = 1,
    candidate_id: str = ...,
    cycle: int = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/candidate/{candidate_id}/totals/',
    description=""" 
This endpoint provides information about a committee's Form 3, Form 3X, or Form 3P financial reports,
which are aggregated by two-year period. We refer to two-year periods as a `cycle`.

The cycle is named after the even-numbered year and includes the year before it. To obtain
totals from 2013 and 2014, you would use 2014. In odd-numbered years, the current cycle
is the next year — for example, in 2015, the current cycle is 2016.

For presidential and Senate candidates, multiple two-year cycles exist between elections.

 """,
    tags=[
        'candidate_information_management',
        'filing_records_management',
        'state_election_office_data_management',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_candidate__candidate_id_totals_(
    sort_nulls_last: Optional[bool] = False,
    cycle: Optional[Cycle] = None,
    page: Optional[int] = 1,
    sort_hide_null: Optional[bool] = False,
    election_full: Optional[bool] = None,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = '-cycle',
    candidate_id: str = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/candidates/',
    description=""" 
Fetch basic information about candidates, and use parameters to filter results to the
candidates you're looking for.

Each result reflects a unique FEC candidate ID. That ID is particular to the candidate for a
particular office sought. If a candidate runs for the same office multiple times, the ID
stays the same. If the same person runs for another office — for example, a House
candidate runs for a Senate office — that candidate will get a unique ID for each office.
 """,
    tags=[
        'candidate_information_management',
        'filing_records_management',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_candidates_(
    office: Optional[Office2] = None,
    candidate_status: Optional[CandidateStatus1] = None,
    cycle: Optional[Cycle] = None,
    district: Optional[District] = None,
    sort_nulls_last: Optional[bool] = False,
    sort_hide_null: Optional[bool] = False,
    year: Optional[str] = None,
    state: Optional[State] = None,
    name: Optional[Name] = None,
    is_active_candidate: Optional[bool] = None,
    q: Optional[Q] = None,
    federal_funds_flag: Optional[bool] = None,
    per_page: Optional[int] = 20,
    candidate_id: Optional[CandidateId] = None,
    min_first_file_date: Optional[date_aliased] = None,
    page: Optional[int] = 1,
    sort: Optional[str] = 'name',
    has_raised_funds: Optional[bool] = None,
    election_year: Optional[ElectionYear] = None,
    api_key: str = 'DEMO_KEY',
    incumbent_challenge: Optional[IncumbentChallenge1] = None,
    sort_null_only: Optional[bool] = False,
    max_first_file_date: Optional[date_aliased] = None,
    party: Optional[Party] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/candidates/search/',
    description=""" 
Fetch basic information about candidates and their principal committees.

Each result reflects a unique FEC candidate ID. That ID is assigned to the candidate for a
particular office sought. If a candidate runs for the same office over time, that ID
stays the same. If the same person runs for multiple offices — for example, a House
candidate runs for a Senate office — that candidate will get a unique ID for each office.

The candidate endpoints primarily use data from FEC registration
[Form 1](https://www.fec.gov/pdf/forms/fecfrm1.pdf) for committee information and
[Form 2](https://www.fec.gov/pdf/forms/fecfrm2.pdf) for candidate information.
 """,
    tags=['candidate_information_management', 'filing_records_management'],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_candidates_search_(
    office: Optional[Office3] = None,
    candidate_status: Optional[CandidateStatus2] = None,
    cycle: Optional[Cycle] = None,
    district: Optional[District] = None,
    sort_nulls_last: Optional[bool] = False,
    sort_hide_null: Optional[bool] = False,
    year: Optional[str] = None,
    state: Optional[State] = None,
    name: Optional[Name] = None,
    is_active_candidate: Optional[bool] = None,
    q: Optional[Q] = None,
    federal_funds_flag: Optional[bool] = None,
    per_page: Optional[int] = 20,
    candidate_id: Optional[CandidateId] = None,
    min_first_file_date: Optional[date_aliased] = None,
    page: Optional[int] = 1,
    sort: Optional[str] = 'name',
    has_raised_funds: Optional[bool] = None,
    election_year: Optional[ElectionYear] = None,
    api_key: str = 'DEMO_KEY',
    incumbent_challenge: Optional[IncumbentChallenge2] = None,
    sort_null_only: Optional[bool] = False,
    max_first_file_date: Optional[date_aliased] = None,
    party: Optional[Party] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/candidates/totals/',
    description=""" 
Aggregated candidate receipts and disbursements grouped by cycle.
 """,
    tags=[
        'candidate_information_management',
        'committee_operations_management',
        'candidate_financial_statistics',
        'financial_totals_summary',
        'financial_metrics_retrieval',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_candidates_totals_(
    office: Optional[Office4] = None,
    min_receipts: Optional[str] = None,
    cycle: Optional[Cycle] = None,
    district: Optional[District] = None,
    max_receipts: Optional[str] = None,
    sort_hide_null: Optional[bool] = False,
    state: Optional[State] = None,
    max_debts_owed_by_committee: Optional[str] = None,
    sort_nulls_last: Optional[bool] = False,
    min_debts_owed_by_committee: Optional[str] = None,
    is_active_candidate: Optional[bool] = None,
    q: Optional[Q] = None,
    federal_funds_flag: Optional[bool] = None,
    per_page: Optional[int] = 20,
    max_cash_on_hand_end_period: Optional[str] = None,
    max_disbursements: Optional[str] = None,
    candidate_id: Optional[CandidateId] = None,
    page: Optional[int] = 1,
    has_raised_funds: Optional[bool] = None,
    election_full: Optional[bool] = True,
    election_year: Optional[ElectionYear] = None,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    min_cash_on_hand_end_period: Optional[str] = None,
    sort: Optional[str] = None,
    party: Optional[Party] = None,
    min_disbursements: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/candidates/totals/aggregates/',
    description="""  Candidate total receipts and disbursements aggregated by `aggregate_by`.
 """,
    tags=[
        'candidate_information_management',
        'election_information_retrieval',
        'filing_records_management',
        'state_election_office_data_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_candidates_totals_aggregates_(
    office: Optional[Office5] = None,
    sort_nulls_last: Optional[bool] = False,
    district: Optional[District] = None,
    state: Optional[State] = None,
    min_election_cycle: Optional[int] = None,
    sort_hide_null: Optional[bool] = False,
    is_active_candidate: Optional[bool] = None,
    per_page: Optional[int] = 20,
    max_election_cycle: Optional[int] = None,
    aggregate_by: Optional[AggregateBy] = None,
    page: Optional[int] = 1,
    election_year: Optional[ElectionYear] = None,
    election_full: Optional[bool] = True,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    sort: Optional[Sort] = ['-election_year'],
    party: Optional[Party5] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/candidates/totals/by_office/',
    description="""  Aggregated candidate receipts and disbursements grouped by office by cycle.
 """,
    tags=[
        'candidate_information_management',
        'election_information_retrieval',
        'filing_records_management',
        'state_election_office_data_management',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_candidates_totals_by_office_(
    office: Optional[Office5] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    min_election_cycle: Optional[int] = None,
    sort_hide_null: Optional[bool] = False,
    election_year: Optional[ElectionYear] = None,
    election_full: Optional[bool] = True,
    api_key: str = 'DEMO_KEY',
    is_active_candidate: Optional[bool] = None,
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    max_election_cycle: Optional[int] = None,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/candidates/totals/by_office/by_party/',
    description="""  Aggregated candidate receipts and disbursements grouped by office by party by cycle.
 """,
    tags=[
        'candidate_information_management',
        'election_information_retrieval',
        'state_election_office_data_management',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_candidates_totals_by_office_by_party_(
    office: Optional[Office5] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    sort_hide_null: Optional[bool] = False,
    election_year: Optional[ElectionYear] = None,
    election_full: Optional[bool] = True,
    api_key: str = 'DEMO_KEY',
    is_active_candidate: Optional[bool] = None,
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/committee/{committee_id}/',
    description=""" 
This endpoint is useful for finding detailed information about a particular committee or
filer. Use the `committee_id` to find the most recent information about the committee.
 """,
    tags=[
        'candidate_information_management',
        'committee_operations_management',
        'filing_records_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_committee__committee_id_(
    designation: Optional[Designation3] = None,
    committee_type: Optional[CommitteeType2] = None,
    page: Optional[int] = 1,
    filing_frequency: Optional[FilingFrequency1] = None,
    cycle: Optional[Cycle] = None,
    sort_hide_null: Optional[bool] = False,
    year: Optional[Year] = None,
    sort_nulls_last: Optional[bool] = False,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    organization_type: Optional[OrganizationType1] = None,
    sort: Optional[str] = 'name',
    committee_id: str = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/committee/{committee_id}/candidates/',
    description=""" 
This endpoint is useful for finding detailed information about a particular candidate. Use the
`candidate_id` to find the most recent information about that candidate.
 """,
    tags=[
        'candidate_information_management',
        'committee_operations_management',
        'candidate_financial_statistics',
        'filing_records_management',
        'state_election_office_data_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_committee__committee_id_candidates_(
    office: Optional[Office11] = None,
    candidate_status: Optional[CandidateStatus3] = None,
    cycle: Optional[Cycle] = None,
    sort_nulls_last: Optional[bool] = False,
    state: Optional[State] = None,
    district: Optional[District] = None,
    year: Optional[str] = None,
    name: Optional[Name] = None,
    sort_hide_null: Optional[bool] = False,
    federal_funds_flag: Optional[bool] = None,
    per_page: Optional[int] = 20,
    page: Optional[int] = 1,
    sort: Optional[str] = 'name',
    has_raised_funds: Optional[bool] = None,
    election_year: Optional[ElectionYear] = None,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    incumbent_challenge: Optional[IncumbentChallenge3] = None,
    party: Optional[Party7] = None,
    committee_id: str = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/committee/{committee_id}/candidates/history/',
    description=""" 
Find out a candidate's characteristics over time. This is particularly useful if the
candidate runs for the same office in different districts or you want to know more about a candidate's
previous races.

This information is organized by `candidate_id`, so it won't help you find a candidate
who ran for different offices over time; candidates get a new ID for each office.
 """,
    tags=[
        'candidate_information_management',
        'committee_operations_management',
        'filing_records_management',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_committee__committee_id_candidates_history_(
    api_key: str = 'DEMO_KEY',
    sort_nulls_last: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort_null_only: Optional[bool] = False,
    sort_hide_null: Optional[bool] = False,
    sort: Optional[str] = '-two_year_period',
    election_full: Optional[bool] = True,
    page: Optional[int] = 1,
    committee_id: str = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/committee/{committee_id}/candidates/history/{cycle}/',
    description=""" 
Find out a candidate's characteristics over time. This is particularly useful if the
candidate runs for the same office in different districts or you want to know more about a candidate's
previous races.

This information is organized by `candidate_id`, so it won't help you find a candidate
who ran for different offices over time; candidates get a new ID for each office.
 """,
    tags=[
        'candidate_information_management',
        'committee_operations_management',
        'election_information_retrieval',
        'filing_records_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_committee__committee_id_candidates_history__cycle_(
    api_key: str = 'DEMO_KEY',
    sort_nulls_last: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort_null_only: Optional[bool] = False,
    sort_hide_null: Optional[bool] = False,
    sort: Optional[str] = '-two_year_period',
    election_full: Optional[bool] = True,
    page: Optional[int] = 1,
    committee_id: str = ...,
    cycle: int = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/committee/{committee_id}/filings/',
    description=""" 
All official records and reports filed by or delivered to the FEC.

Note: because the filings data includes many records, counts for large
result sets are approximate; you will want to page through the records until no records are returned.
 """,
    tags=[
        'filing_records_management',
        'electronic_filing_reports_management',
        'committee_operations_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_committee__committee_id_filings_(
    office: Optional[Office12] = None,
    file_number: Optional[FileNumber] = None,
    form_category: Optional[FormCategory] = None,
    cycle: Optional[Cycle] = None,
    district: Optional[District] = None,
    document_type: Optional[DocumentType] = None,
    sort_hide_null: Optional[bool] = False,
    state: Optional[State] = None,
    amendment_indicator: Optional[AmendmentIndicator1] = None,
    sort_nulls_last: Optional[bool] = False,
    is_amended: Optional[bool] = None,
    filer_type: Optional[FilerType] = None,
    max_receipt_date: Optional[date_aliased] = None,
    per_page: Optional[int] = 20,
    primary_general_indicator: Optional[PrimaryGeneralIndicator] = None,
    request_type: Optional[RequestType] = None,
    report_year: Optional[ReportYear] = None,
    form_type: Optional[FormType] = None,
    committee_type: Optional[str] = None,
    beginning_image_number: Optional[BeginningImageNumber] = None,
    page: Optional[int] = 1,
    sort: Optional[Sort] = ['-receipt_date'],
    api_key: str = 'DEMO_KEY',
    min_receipt_date: Optional[date_aliased] = None,
    sort_null_only: Optional[bool] = False,
    q_filer: Optional[QFiler] = None,
    report_type: Optional[ReportType1] = None,
    party: Optional[Party7] = None,
    most_recent: Optional[bool] = None,
    committee_id: str = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/committee/{committee_id}/history/',
    description=""" 
Explore a filer's characteristics over time. This can be particularly useful if the
committees change treasurers, designation, or `committee_type`.
 """,
    tags=[
        'candidate_information_management',
        'committee_operations_management',
        'filing_records_management',
        'election_information_retrieval',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_committee__committee_id_history_(
    designation: Optional[Designation4] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    sort_hide_null: Optional[bool] = False,
    election_full: Optional[bool] = True,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = '-cycle',
    committee_id: str = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/committee/{committee_id}/history/{cycle}/',
    description=""" 
Explore a filer's characteristics over time. This can be particularly useful if the
committees change treasurers, designation, or `committee_type`.
 """,
    tags=[
        'committee_operations_management',
        'candidate_financial_statistics',
        'filing_records_management',
        'financial_metrics_retrieval',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_committee__committee_id_history__cycle_(
    designation: Optional[Designation5] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    sort_hide_null: Optional[bool] = False,
    election_full: Optional[bool] = True,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = '-cycle',
    committee_id: str = ...,
    cycle: int = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/committee/{committee_id}/reports/',
    description=""" 
Each report represents the summary information from Form 3, Form 3X and Form 3P.
These reports have key statistics that illuminate the financial status of a given committee.
Things like cash on hand, debts owed by committee, total receipts, and total disbursements
are especially helpful for understanding a committee's financial dealings.

By default, this endpoint includes both amended and final versions of each report. To restrict
to only the final versions of each report, use `is_amended=false`; to retrieve only reports that
have been amended, use `is_amended=true`.

Several different reporting structures exist, depending on the type of organization that
submits financial information. To see an example of these reporting requirements,
look at the summary and detailed summary pages of Form 3, Form 3X, and Form 3P.

DISCLAIMER: The field labels contained within this resource are subject to change.  We are attempting to succinctly
label these fields while conveying clear meaning to ensure accessibility for all users.
 """,
    tags=[
        'candidate_financial_statistics',
        'committee_operations_management',
        'schedule_b_disbursement_management',
        'schedule_e_expenditure_management',
        'financial_totals_summary',
        'financial_metrics_retrieval',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_committee__committee_id_reports_(
    min_debts_owed_amount: Optional[str] = None,
    max_disbursements_amount: Optional[str] = None,
    max_total_contributions: Optional[str] = None,
    max_debts_owed_expenditures: Optional[str] = None,
    cycle: Optional[Cycle] = None,
    sort_nulls_last: Optional[bool] = False,
    sort_hide_null: Optional[bool] = False,
    year: Optional[Year] = None,
    max_receipts_amount: Optional[str] = None,
    max_cash_on_hand_end_period_amount: Optional[str] = None,
    is_amended: Optional[bool] = None,
    min_disbursements_amount: Optional[str] = None,
    max_party_coordinated_expenditures: Optional[str] = None,
    per_page: Optional[int] = 20,
    max_independent_expenditures: Optional[str] = None,
    min_receipts_amount: Optional[str] = None,
    min_party_coordinated_expenditures: Optional[str] = None,
    candidate_id: Optional[str] = None,
    beginning_image_number: Optional[BeginningImageNumber] = None,
    page: Optional[int] = 1,
    type: Optional[Type] = None,
    sort: Optional[Sort] = ['-coverage_end_date'],
    min_total_contributions: Optional[str] = None,
    api_key: str = 'DEMO_KEY',
    min_cash_on_hand_end_period_amount: Optional[str] = None,
    sort_null_only: Optional[bool] = False,
    min_independent_expenditures: Optional[str] = None,
    report_type: Optional[ReportType1] = None,
    committee_id: str = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/committee/{committee_id}/totals/',
    description=""" 
This endpoint provides information about a committee's Form 3, Form 3X, or Form 3P financial reports,
which are aggregated by two-year period. We refer to two-year periods as a `cycle`.

The cycle is named after the even-numbered year and includes the year before it. To obtain
totals from 2013 and 2014, you would use 2014. In odd-numbered years, the current cycle
is the next year — for example, in 2015, the current cycle is 2016.

For presidential and Senate candidates, multiple two-year cycles exist between elections.

 """,
    tags=[
        'committee_operations_management',
        'candidate_information_management',
        'filing_records_management',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_committee__committee_id_totals_(
    cycle: Optional[Cycle] = None,
    sort_nulls_last: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort_null_only: Optional[bool] = False,
    sort_hide_null: Optional[bool] = False,
    sort: Optional[str] = '-cycle',
    api_key: str = 'DEMO_KEY',
    page: Optional[int] = 1,
    committee_id: str = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/committees/',
    description=""" 
Fetch basic information about committees and filers. Use parameters to filter for
particular characteristics.

 """,
    tags=[
        'candidate_information_management',
        'committee_operations_management',
        'filing_records_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_committees_(
    designation: Optional[Designation6] = None,
    max_first_f1_date: Optional[date_aliased] = None,
    min_first_f1_date: Optional[date_aliased] = None,
    cycle: Optional[Cycle] = None,
    filing_frequency: Optional[FilingFrequency2] = None,
    sort_nulls_last: Optional[bool] = False,
    sort_hide_null: Optional[bool] = False,
    year: Optional[Year] = None,
    state: Optional[State] = None,
    sponsor_candidate_id: Optional[SponsorCandidateId] = None,
    q: Optional[Q] = None,
    per_page: Optional[int] = 20,
    min_last_f1_date: Optional[date_aliased] = None,
    candidate_id: Optional[CandidateId] = None,
    committee_type: Optional[CommitteeType3] = None,
    min_first_file_date: Optional[date_aliased] = None,
    page: Optional[int] = 1,
    sort: Optional[str] = 'name',
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    max_last_f1_date: Optional[date_aliased] = None,
    treasurer_name: Optional[TreasurerName] = None,
    organization_type: Optional[OrganizationType2] = None,
    max_first_file_date: Optional[date_aliased] = None,
    party: Optional[Party7] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/communication_costs/',
    description=""" 
52 U.S.C. 30118 allows "communications by a corporation to its stockholders and executive or administrative personnel and their families or by a labor organization to its members and their families on any subject," including the express advocacy of the election or defeat of any Federal candidate.  The costs of such communications must be reported to the Federal Election Commission under certain circumstances.
 """,
    tags=[
        'candidate_information_management',
        'committee_operations_management',
        'candidate_financial_statistics',
        'filing_records_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_communication_costs_(
    max_amount: Optional[str] = None,
    max_image_number: Optional[str] = None,
    sort_nulls_last: Optional[bool] = False,
    support_oppose_indicator: Optional[SupportOpposeIndicator] = None,
    sort_hide_null: Optional[bool] = False,
    line_number: Optional[str] = None,
    per_page: Optional[int] = 20,
    candidate_id: Optional[CandidateId] = None,
    page: Optional[int] = 1,
    min_date: Optional[date_aliased] = None,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    min_amount: Optional[str] = None,
    min_image_number: Optional[str] = None,
    sort_null_only: Optional[bool] = False,
    image_number: Optional[ImageNumber] = None,
    sort: Optional[str] = None,
    max_date: Optional[date_aliased] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/communication_costs/aggregates/',
    description=""" Communication cost aggregated by candidate ID and committee ID. """,
    tags=[
        'candidate_information_management',
        'committee_operations_management',
        'filing_records_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_communication_costs_aggregates_(
    candidate_id: Optional[CandidateId] = None,
    page: Optional[int] = 1,
    cycle: Optional[Cycle] = None,
    support_oppose_indicator: Optional[SupportOpposeIndicator1] = None,
    sort_hide_null: Optional[bool] = False,
    sort_nulls_last: Optional[bool] = False,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/communication_costs/by_candidate/',
    description=""" Communication cost aggregated by candidate ID and committee ID. """,
    tags=[
        'candidate_information_management',
        'election_information_retrieval',
        'state_election_office_data_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_communication_costs_by_candidate_(
    office: Optional[Office13] = None,
    candidate_id: Optional[CandidateId] = None,
    cycle: Optional[Cycle] = None,
    district: Optional[str] = None,
    state: Optional[str] = None,
    page: Optional[int] = 1,
    support_oppose: Optional[SupportOppose] = None,
    election_full: Optional[bool] = True,
    sort_hide_null: Optional[bool] = False,
    sort_nulls_last: Optional[bool] = False,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/communication_costs/totals/by_candidate/',
    description=""" 
Total communications costs aggregated across committees on supported or opposed candidates by cycle or candidate election year.
 """,
    tags=[
        'candidate_information_management',
        'filing_records_management',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_communication_costs_totals_by_candidate_(
    candidate_id: Optional[CandidateId] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    cycle: Optional[Cycle] = None,
    sort_hide_null: Optional[bool] = False,
    election_full: Optional[bool] = True,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/efile/filings/',
    description=""" Basic information about electronic files coming into the FEC, posted as they are received. """,
    tags=['filing_records_management', 'committee_operations_management'],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_efile_filings_(
    file_number: Optional[FileNumber] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    per_page: Optional[int] = 20,
    sort_hide_null: Optional[bool] = False,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    min_receipt_date: Optional[date_aliased] = None,
    max_receipt_date: Optional[date_aliased] = None,
    q_filer: Optional[QFiler] = None,
    sort_null_only: Optional[bool] = False,
    sort: Optional[str] = '-receipt_date',
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/efile/reports/house-senate/',
    description=""" 
Key financial data reported periodically by committees as they are reported. This feed includes summary
information from the the House F3 reports, the presidential F3p reports and the PAC and party
F3x reports.

Generally, committees file reports on a quarterly or monthly basis, but
some must also submit a report 12 days before primary elections. Therefore, during the primary
season, the period covered by this file may be different for different committees. These totals
also incorporate any changes made by committees, if any report covering the period is amended.

DISCLAIMER: The field labels contained within this resource are subject to change.  We are attempting to succinctly
label these fields while conveying clear meaning to ensure accessibility for all users.
 """,
    tags=['filing_records_management', 'committee_operations_management'],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_efile_reports_house_senate_(
    file_number: Optional[FileNumber] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    per_page: Optional[int] = 20,
    sort_hide_null: Optional[bool] = False,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    min_receipt_date: Optional[date_aliased] = None,
    max_receipt_date: Optional[date_aliased] = None,
    q_filer: Optional[QFiler] = None,
    sort_null_only: Optional[bool] = False,
    sort: Optional[str] = '-receipt_date',
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/efile/reports/pac-party/',
    description=""" 
Key financial data reported periodically by committees as they are reported. This feed includes summary
information from the the House F3 reports, the presidential F3p reports and the PAC and party
F3x reports.

Generally, committees file reports on a quarterly or monthly basis, but
some must also submit a report 12 days before primary elections. Therefore, during the primary
season, the period covered by this file may be different for different committees. These totals
also incorporate any changes made by committees, if any report covering the period is amended.

DISCLAIMER: The field labels contained within this resource are subject to change.  We are attempting to succinctly
label these fields while conveying clear meaning to ensure accessibility for all users.
 """,
    tags=[
        'filing_records_management',
        'committee_operations_management',
        'schedule_e_expenditure_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_efile_reports_pac_party_(
    file_number: Optional[FileNumber] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    per_page: Optional[int] = 20,
    sort_hide_null: Optional[bool] = False,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    min_receipt_date: Optional[date_aliased] = None,
    max_receipt_date: Optional[date_aliased] = None,
    q_filer: Optional[QFiler] = None,
    sort_null_only: Optional[bool] = False,
    sort: Optional[str] = '-receipt_date',
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/efile/reports/presidential/',
    description=""" 
Key financial data reported periodically by committees as they are reported. This feed includes summary
information from the the House F3 reports, the presidential F3p reports and the PAC and party
F3x reports.

Generally, committees file reports on a quarterly or monthly basis, but
some must also submit a report 12 days before primary elections. Therefore, during the primary
season, the period covered by this file may be different for different committees. These totals
also incorporate any changes made by committees, if any report covering the period is amended.

DISCLAIMER: The field labels contained within this resource are subject to change.  We are attempting to succinctly
label these fields while conveying clear meaning to ensure accessibility for all users.
 """,
    tags=['filing_records_management', 'committee_operations_management'],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_efile_reports_presidential_(
    file_number: Optional[FileNumber] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    per_page: Optional[int] = 20,
    sort_hide_null: Optional[bool] = False,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    min_receipt_date: Optional[date_aliased] = None,
    max_receipt_date: Optional[date_aliased] = None,
    q_filer: Optional[QFiler] = None,
    sort_null_only: Optional[bool] = False,
    sort: Optional[str] = '-receipt_date',
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/election-dates/',
    description=""" 
FEC election dates since 1995.
 """,
    tags=['election_information_retrieval', 'filing_records_management'],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_election_dates_(
    max_update_date: Optional[date_aliased] = None,
    sort_nulls_last: Optional[bool] = False,
    sort_hide_null: Optional[bool] = False,
    max_primary_general_date: Optional[date_aliased] = None,
    min_update_date: Optional[date_aliased] = None,
    max_create_date: Optional[date_aliased] = None,
    per_page: Optional[int] = 20,
    min_election_date: Optional[date_aliased] = None,
    election_party: Optional[ElectionParty] = None,
    election_type_id: Optional[ElectionTypeId] = None,
    page: Optional[int] = 1,
    min_create_date: Optional[date_aliased] = None,
    min_primary_general_date: Optional[date_aliased] = None,
    election_year: Optional[ElectionYear8] = None,
    api_key: str = 'DEMO_KEY',
    election_district: Optional[ElectionDistrict] = None,
    office_sought: Optional[OfficeSought] = None,
    sort_null_only: Optional[bool] = False,
    max_election_date: Optional[date_aliased] = None,
    sort: Optional[str] = '-election_date',
    election_state: Optional[ElectionState] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/electioneering/',
    description=""" 
An electioneering communication is any broadcast, cable or satellite communication that fulfills each of the following conditions:

_The communication refers to a clearly identified federal candidate._

_The communication is publicly distributed by a television station, radio station, cable television system or satellite system for a fee._

_The communication is distributed within 60 days prior to a general election or 30 days prior to a primary election to federal office._
 """,
    tags=[
        'candidate_information_management',
        'committee_operations_management',
        'candidate_financial_statistics',
        'filing_records_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_electioneering_(
    candidate_id: Optional[CandidateId] = None,
    max_amount: Optional[str] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    sort_hide_null: Optional[bool] = False,
    committee_id: Optional[CommitteeId] = None,
    min_date: Optional[date_aliased] = None,
    api_key: str = 'DEMO_KEY',
    min_amount: Optional[str] = None,
    per_page: Optional[int] = 20,
    sort_null_only: Optional[bool] = False,
    description: Optional[str] = None,
    sort: Optional[str] = None,
    report_year: Optional[ReportYear] = None,
    last_index: Optional[int] = None,
    max_date: Optional[date_aliased] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/electioneering/aggregates/',
    description=""" Electioneering communications costs aggregates """,
    tags=[
        'candidate_information_management',
        'committee_operations_management',
        'filing_records_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_electioneering_aggregates_(
    candidate_id: Optional[CandidateId] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    cycle: Optional[Cycle] = None,
    sort_hide_null: Optional[bool] = False,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/electioneering/by_candidate/',
    description=""" Electioneering costs aggregated by candidate """,
    tags=['candidate_information_management', 'state_election_office_data_management'],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_electioneering_by_candidate_(
    office: Optional[Office13] = None,
    candidate_id: Optional[CandidateId] = None,
    cycle: Optional[Cycle] = None,
    district: Optional[str] = None,
    state: Optional[str] = None,
    page: Optional[int] = 1,
    sort_hide_null: Optional[bool] = False,
    election_full: Optional[bool] = True,
    sort_nulls_last: Optional[bool] = False,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/electioneering/totals/by_candidate/',
    description=""" 
Total electioneering communications spent on candidates by cycle
or candidate election year
 """,
    tags=[
        'candidate_information_management',
        'election_information_retrieval',
        'filing_records_management',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_electioneering_totals_by_candidate_(
    candidate_id: Optional[CandidateId] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    cycle: Optional[Cycle] = None,
    sort_hide_null: Optional[bool] = False,
    election_full: Optional[bool] = True,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/elections/',
    description=""" 
Look at the top-level financial information for all candidates running for the same
office.

Choose a 2-year cycle, and `house`, `senate` or `presidential`.

If you are looking for a Senate seat, you will need to select the state using a two-letter
abbreviation.

House races require state and a two-digit district number.

Since this endpoint reflects financial information, it will only have candidates once they file
financial reporting forms. Query the `/candidates` endpoint to retrieve an-up-to-date list of all the
candidates that filed to run for a particular seat.
 """,
    tags=[
        'state_election_office_data_management',
        'filing_records_management',
        'election_information_retrieval',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_elections_(
    office: Office13,
    page: Optional[int] = 1,
    district: Optional[str] = None,
    state: Optional[str] = None,
    cycle: int = ...,
    sort_hide_null: Optional[bool] = False,
    election_full: Optional[bool] = True,
    sort_nulls_last: Optional[bool] = False,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = '-total_receipts',
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/elections/search/',
    description=""" 
List elections by cycle, office, state, and district.
 """,
    tags=['state_election_office_data_management', 'filing_records_management'],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_elections_search_(
    office: Optional[Office19] = None,
    cycle: Optional[Cycle] = None,
    district: Optional[District] = None,
    zip: Optional[Zip] = None,
    state: Optional[State] = None,
    sort_hide_null: Optional[bool] = False,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[Sort] = ['sort_order', 'district'],
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/elections/summary/',
    description=""" 
List elections by cycle, office, state, and district.
 """,
    tags=['election_information_retrieval', 'state_election_office_data_management'],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_elections_summary_(
    office: Office20,
    cycle: int = ...,
    district: Optional[str] = None,
    state: Optional[str] = None,
    api_key: str = 'DEMO_KEY',
    election_full: Optional[bool] = True,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/filings/',
    description=""" 
All official records and reports filed by or delivered to the FEC.

Note: because the filings data includes many records, counts for large
result sets are approximate; you will want to page through the records until no records are returned.
 """,
    tags=[
        'filing_records_management',
        'candidate_information_management',
        'committee_operations_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_filings_(
    office: Optional[Office22] = None,
    file_number: Optional[FileNumber] = None,
    form_category: Optional[FormCategory] = None,
    cycle: Optional[Cycle] = None,
    district: Optional[District] = None,
    document_type: Optional[DocumentType] = None,
    sort_hide_null: Optional[bool] = False,
    state: Optional[State] = None,
    amendment_indicator: Optional[AmendmentIndicator2] = None,
    sort_nulls_last: Optional[bool] = False,
    is_amended: Optional[bool] = None,
    filer_type: Optional[FilerType] = None,
    max_receipt_date: Optional[date_aliased] = None,
    per_page: Optional[int] = 20,
    primary_general_indicator: Optional[PrimaryGeneralIndicator] = None,
    request_type: Optional[RequestType] = None,
    report_year: Optional[ReportYear] = None,
    form_type: Optional[FormType] = None,
    candidate_id: Optional[CandidateId] = None,
    committee_type: Optional[str] = None,
    beginning_image_number: Optional[BeginningImageNumber] = None,
    page: Optional[int] = 1,
    sort: Optional[Sort] = ['-receipt_date'],
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    min_receipt_date: Optional[date_aliased] = None,
    sort_null_only: Optional[bool] = False,
    q_filer: Optional[QFiler] = None,
    report_type: Optional[ReportType1] = None,
    party: Optional[Party7] = None,
    most_recent: Optional[bool] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/legal/search/',
    description=""" 
Search legal documents by document type, or across all document types using keywords, parameter values and ranges.
 """,
    tags=['audit_case_management', 'legal_case_search', 'filing_records_management'],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_legal_search_(
    hits_returned: Optional[int] = None,
    af_report_year: Optional[str] = None,
    case_max_open_date: Optional[date_aliased] = None,
    ao_max_issue_date: Optional[date_aliased] = None,
    case_statutory_citation: Optional[CaseStatutoryCitation] = None,
    case_respondents: Optional[str] = None,
    q: Optional[str] = None,
    ao_min_issue_date: Optional[date_aliased] = None,
    af_max_fd_date: Optional[date_aliased] = None,
    from_hit: Optional[int] = None,
    af_fd_fine_amount: Optional[int] = None,
    type: Optional[str] = None,
    api_key: str = 'DEMO_KEY',
    af_name: Optional[AfName] = None,
    ao_requestor_type: Optional[AoRequestorType] = None,
    ao_statutory_citation: Optional[AoStatutoryCitation] = None,
    ao_entity_name: Optional[AoEntityName] = None,
    mur_type: Optional[str] = None,
    ao_regulatory_citation: Optional[AoRegulatoryCitation] = None,
    af_committee_id: Optional[str] = None,
    ao_requestor: Optional[str] = None,
    case_citation_require_all: Optional[bool] = None,
    af_min_fd_date: Optional[date_aliased] = None,
    ao_is_pending: Optional[bool] = None,
    af_rtb_fine_amount: Optional[int] = None,
    case_election_cycles: Optional[int] = None,
    ao_category: Optional[AoCategory] = None,
    ao_citation_require_all: Optional[bool] = None,
    case_dispositions: Optional[CaseDispositions] = None,
    af_max_rtb_date: Optional[date_aliased] = None,
    case_min_open_date: Optional[date_aliased] = None,
    case_max_close_date: Optional[date_aliased] = None,
    ao_min_request_date: Optional[date_aliased] = None,
    ao_status: Optional[str] = None,
    case_doc_category_id: Optional[CaseDocCategoryId] = None,
    af_min_rtb_date: Optional[date_aliased] = None,
    ao_name: Optional[AoName] = None,
    case_regulatory_citation: Optional[CaseRegulatoryCitation] = None,
    ao_no: Optional[AoNo] = None,
    case_min_close_date: Optional[date_aliased] = None,
    sort: Optional[str] = None,
    ao_max_request_date: Optional[date_aliased] = None,
    case_no: Optional[CaseNo] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/names/audit_candidates/',
    description=""" 
Search for candidates or committees by name. If you're looking for information on a
particular person or group, using a name to find the `candidate_id` or `committee_id` on
this endpoint can be a helpful first step.
 """,
    tags=[
        'electronic_filing_reports_management',
        'filing_records_management',
        'transaction_operations_logging',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_names_audit_candidates_(api_key: str = 'DEMO_KEY', q: Q = ...):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/names/audit_committees/',
    description=""" 
Search for candidates or committees by name. If you're looking for information on a
particular person or group, using a name to find the `candidate_id` or `committee_id` on
this endpoint can be a helpful first step.
 """,
    tags=[
        'candidate_committee_audit',
        'filing_records_management',
        'transaction_operations_logging',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_names_audit_committees_(api_key: str = 'DEMO_KEY', q: Q = ...):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/names/candidates/',
    description=""" 
Search for candidates or committees by name. If you're looking for information on a
particular person or group, using a name to find the `candidate_id` or `committee_id` on
this endpoint can be a helpful first step.
 """,
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_names_candidates_(api_key: str = 'DEMO_KEY', q: Q = ...):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/names/committees/',
    description=""" 
Search for candidates or committees by name. If you're looking for information on a
particular person or group, using a name to find the `candidate_id` or `committee_id` on
this endpoint can be a helpful first step.
 """,
    tags=['candidate_information_management'],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_names_committees_(api_key: str = 'DEMO_KEY', q: Q = ...):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/operations-log/',
    description=""" 
The Operations log contains details of each report loaded into the database. It is primarily
used as status check to determine when all of the data processes, from initial entry through
review are complete.
 """,
    tags=[
        'candidate_information_management',
        'committee_operations_management',
        'filing_records_management',
        'transaction_operations_logging',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_operations_log_(
    max_coverage_end_date: Optional[date_aliased] = None,
    sort_nulls_last: Optional[bool] = False,
    amendment_indicator: Optional[AmendmentIndicator3] = None,
    sort_hide_null: Optional[bool] = False,
    max_receipt_date: Optional[date_aliased] = None,
    per_page: Optional[int] = 20,
    min_transaction_data_complete_date: Optional[date_aliased] = None,
    form_type: Optional[FormType] = None,
    max_transaction_data_complete_date: Optional[date_aliased] = None,
    beginning_image_number: Optional[BeginningImageNumber] = None,
    page: Optional[int] = 1,
    min_coverage_end_date: Optional[date_aliased] = None,
    sort: Optional[Sort] = ['-report_year'],
    api_key: str = 'DEMO_KEY',
    min_receipt_date: Optional[date_aliased] = None,
    sort_null_only: Optional[bool] = False,
    candidate_committee_id: Optional[CandidateCommitteeId] = None,
    report_type: Optional[ReportType1] = None,
    report_year: Optional[ReportYear] = None,
    status_num: Optional[StatusNum] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/presidential/contributions/by_candidate/',
    description=""" 
Net receipts per candidate.

Filter with `contributor_state='US'` for national totals
 """,
    tags=[
        'candidate_financial_statistics',
        'financial_totals_summary',
        'financial_metrics_retrieval',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_presidential_contributions_by_candidate_(
    contributor_state: Optional[ContributorState] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    sort_hide_null: Optional[bool] = False,
    election_year: Optional[ElectionYear9] = None,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = '-net_receipts',
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/presidential/contributions/by_size/',
    description=""" 
Contribution receipts by size per candidate.

Filter by candidate_id, election_year and/or size
 """,
    tags=[
        'candidate_information_management',
        'filing_records_management',
        'election_information_retrieval',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_presidential_contributions_by_size_(
    candidate_id: Optional[CandidateId] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    sort_hide_null: Optional[bool] = False,
    election_year: Optional[ElectionYear9] = None,
    api_key: str = 'DEMO_KEY',
    size: Optional[Size] = None,
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = 'size',
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/presidential/contributions/by_state/',
    description=""" 
Contribution receipts by state per candidate.

Filter by candidate_id and/or election_year
 """,
    tags=[
        'candidate_information_management',
        'candidate_financial_statistics',
        'filing_records_management',
        'schedule_a_contribution_management',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_presidential_contributions_by_state_(
    candidate_id: Optional[CandidateId] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    sort_hide_null: Optional[bool] = False,
    election_year: Optional[ElectionYear9] = None,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = '-contribution_receipt_amount',
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/presidential/coverage_end_date/',
    description=""" 
Coverage end date per candidate.

Filter by candidate_id and/or election_year
 """,
    tags=[
        'candidate_information_management',
        'filing_records_management',
        'election_information_retrieval',
        'state_election_office_data_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_presidential_coverage_end_date_(
    candidate_id: Optional[CandidateId] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    sort_hide_null: Optional[bool] = False,
    election_year: Optional[ElectionYear9] = None,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = 'candidate_id',
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/presidential/financial_summary/',
    description=""" 
Financial summary per candidate.

Filter by candidate_id and/or election_year
 """,
    tags=[
        'candidate_information_management',
        'candidate_financial_statistics',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_presidential_financial_summary_(
    candidate_id: Optional[CandidateId] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    sort_hide_null: Optional[bool] = False,
    election_year: Optional[ElectionYear9] = None,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = '-net_receipts',
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/rad-analyst/',
    description=""" 
Use this endpoint to look up the RAD Analyst for a committee.

The mission of the Reports Analysis Division (RAD) is to ensure that
campaigns and political committees file timely and accurate reports that fully disclose
their financial activities.  RAD is responsible for reviewing statements and financial
reports filed by political committees participating in federal elections, providing
assistance and guidance to the committees to properly file their reports, and for taking
appropriate action to ensure compliance with the Federal Election Campaign Act (FECA).
 """,
    tags=['filing_records_management', 'committee_operations_management'],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_rad_analyst_(
    min_assignment_update_date: Optional[date_aliased] = None,
    max_assignment_update_date: Optional[date_aliased] = None,
    analyst_short_id: Optional[AnalystShortId] = None,
    email: Optional[Email] = None,
    page: Optional[int] = 1,
    sort_nulls_last: Optional[bool] = False,
    title: Optional[Title] = None,
    sort_hide_null: Optional[bool] = False,
    committee_id: Optional[CommitteeId] = None,
    name: Optional[Name] = None,
    telephone_ext: Optional[TelephoneExt] = None,
    api_key: str = 'DEMO_KEY',
    analyst_id: Optional[AnalystId] = None,
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/reporting-dates/',
    description=""" 
FEC election dates since 1995.
 """,
    tags=['filing_records_management', 'state_election_office_data_management'],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_reporting_dates_(
    max_update_date: Optional[date_aliased] = None,
    page: Optional[int] = 1,
    max_due_date: Optional[date_aliased] = None,
    min_create_date: Optional[date_aliased] = None,
    min_due_date: Optional[date_aliased] = None,
    sort_hide_null: Optional[bool] = False,
    sort_nulls_last: Optional[bool] = False,
    api_key: str = 'DEMO_KEY',
    min_update_date: Optional[date_aliased] = None,
    max_create_date: Optional[date_aliased] = None,
    per_page: Optional[int] = 20,
    sort_null_only: Optional[bool] = False,
    sort: Optional[str] = '-due_date',
    report_year: Optional[ReportYear] = None,
    report_type: Optional[ReportType1] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/reports/{entity_type}/',
    description=""" 
Each report represents the summary information from Form 3, Form 3X and Form 3P.
These reports have key statistics that illuminate the financial status of a given committee.
Things like cash on hand, debts owed by committee, total receipts, and total disbursements
are especially helpful for understanding a committee's financial dealings.

By default, this endpoint includes both amended and final versions of each report. To restrict
to only the final versions of each report, use `is_amended=false`; to retrieve only reports that
have been amended, use `is_amended=true`.

Several different reporting structures exist, depending on the type of organization that
submits financial information. To see an example of these reporting requirements,
look at the summary and detailed summary pages of Form 3, Form 3X, and Form 3P.

DISCLAIMER: The field labels contained within this resource are subject to change.  We are attempting to succinctly
label these fields while conveying clear meaning to ensure accessibility for all users.
 """,
    tags=[
        'candidate_financial_statistics',
        'committee_operations_management',
        'filing_records_management',
        'schedule_a_contribution_management',
        'schedule_b_disbursement_management',
        'schedule_c_debt_management',
        'schedule_d_outstanding_debt_management',
        'schedule_e_expenditure_management',
        'financial_totals_summary',
        'financial_metrics_retrieval',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_reports__entity_type_(
    min_debts_owed_amount: Optional[str] = None,
    max_debts_owed_expenditures: Optional[str] = None,
    sort_hide_null: Optional[bool] = False,
    year: Optional[Year] = None,
    max_cash_on_hand_end_period_amount: Optional[str] = None,
    filer_type: Optional[FilerType] = None,
    max_party_coordinated_expenditures: Optional[str] = None,
    q_spender: Optional[QSpender] = None,
    max_receipt_date: Optional[date_aliased] = None,
    per_page: Optional[int] = 20,
    max_independent_expenditures: Optional[str] = None,
    min_party_coordinated_expenditures: Optional[str] = None,
    committee_type: Optional[CommitteeType4] = None,
    page: Optional[int] = 1,
    min_total_contributions: Optional[str] = None,
    api_key: str = 'DEMO_KEY',
    min_cash_on_hand_end_period_amount: Optional[str] = None,
    min_receipt_date: Optional[date_aliased] = None,
    sort_null_only: Optional[bool] = False,
    min_independent_expenditures: Optional[str] = None,
    q_filer: Optional[QFiler] = None,
    max_disbursements_amount: Optional[str] = None,
    max_total_contributions: Optional[str] = None,
    cycle: Optional[Cycle] = None,
    amendment_indicator: Optional[AmendmentIndicator4] = None,
    sort_nulls_last: Optional[bool] = False,
    max_receipts_amount: Optional[str] = None,
    is_amended: Optional[bool] = None,
    min_disbursements_amount: Optional[str] = None,
    min_receipts_amount: Optional[str] = None,
    candidate_id: Optional[str] = None,
    beginning_image_number: Optional[BeginningImageNumber] = None,
    sort: Optional[Sort] = ['-coverage_end_date'],
    committee_id: Optional[CommitteeId] = None,
    report_type: Optional[ReportType1] = None,
    most_recent: Optional[bool] = None,
    entity_type: EntityType = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_a/',
    description=""" 
This description is for both ​`/schedules​/schedule_a​/` and ​ `/schedules​/schedule_a​/{sub_id}​/`.

This endpoint provides itemized receipts. Schedule A records describe itemized receipts, including contributions from individuals. If you are interested in contributions from an individual, use the `/schedules/schedule_a/` endpoint. For a more complete description of all Schedule A records visit [About receipts data](https://www.fec.gov/campaign-finance-data/about-campaign-finance-data/about-receipts-data/). If you are interested in our "is_individual" methodology visit our [methodology page](https://www.fec.gov/campaign-finance-data/about-campaign-finance-data/methodology/).

​The `/schedules​/schedule_a​/` endpoint is not paginated by page number. This endpoint uses keyset pagination to improve query performance and these indices are required to properly page through this large dataset. To request the next page, you should append the values found in the `last_indexes` object from pagination to the URL of your last request as additional parameters. 
For example, when sorting by `contribution_receipt_date`, you might receive a page of results with the two scenarios of following pagination information:

case #1:
```
pagination: {
    pages: 2152643,
    per_page: 20,
    count: 43052850,
    last_indexes: {
        last_index: "230880619",
        last_contribution_receipt_date: "2014-01-01"
    }
}
```
<br/>
case #2 (results which include contribution_receipt_date = NULL):

```
pagination: {
    pages: 2152644,
    per_page: 20,
    count: 43052850,
    last_indexes: {
        last_index: "230880639",
        sort_null_only: True
    }
}
```
To fetch the next page of sorted results, append `last_index=230880619` and `last_contribution_receipt_date=2014-01-01` to the URL and when reaching `contribution_receipt_date=NULL`, append `last_index=230880639` and `sort_null_only=True`. We strongly advise paging through these results using sort indices. The default sort is acending by `contribution_receipt_date` (`deprecated`, will be descending). If you do not page using sort indices, some transactions may be unintentionally filtered out.

Calls to ​`/schedules​/schedule_a​/` may return many records. For large result sets, the record counts found in the pagination object are approximate; you will need to page through the records until no records are returned.

To avoid throwing the "out of range" exception on the last page, one recommandation is to use total count and `per_page` to control the traverse loop of results.

​The `/schedules​/schedule_a​/{sub_id}​/` endpoint returns a single transaction, but it does include a pagination object class. Please ignore the information in that object class.

 """,
    tags=[
        'schedule_a_contribution_management',
        'financial_metrics_retrieval',
        'candidate_information_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_a_(
    max_load_date: Optional[date_aliased] = None,
    max_amount: Optional[str] = None,
    max_image_number: Optional[str] = None,
    contributor_occupation: Optional[ContributorOccupation] = None,
    recipient_committee_designation: Optional[RecipientCommitteeDesignation] = None,
    sort_hide_null: Optional[bool] = False,
    is_individual: Optional[bool] = None,
    line_number: Optional[str] = None,
    contributor_type: Optional[ContributorType] = None,
    recipient_committee_type: Optional[RecipientCommitteeType] = None,
    per_page: Optional[int] = 20,
    contributor_zip: Optional[ContributorZip] = None,
    contributor_city: Optional[ContributorCity] = None,
    last_index: Optional[int] = None,
    last_contribution_receipt_amount: Optional[float] = None,
    contributor_id: Optional[ContributorId] = None,
    recipient_committee_org_type: Optional[RecipientCommitteeOrgType] = None,
    contributor_state: Optional[ContributorState] = None,
    sort: Optional[str] = '-contribution_receipt_date',
    two_year_transaction_period: Optional[TwoYearTransactionPeriod] = None,
    min_date: Optional[date_aliased] = None,
    committee_id: Optional[CommitteeId] = None,
    contributor_employer: Optional[ContributorEmployer] = None,
    min_load_date: Optional[date_aliased] = None,
    api_key: str = 'DEMO_KEY',
    last_contribution_receipt_date: Optional[date_aliased] = None,
    min_image_number: Optional[str] = None,
    contributor_name: Optional[ContributorName] = None,
    sort_null_only: Optional[bool] = False,
    image_number: Optional[ImageNumber] = None,
    min_amount: Optional[str] = None,
    max_date: Optional[date_aliased] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_a/by_employer/',
    description=""" 
This endpoint provides itemized individual contributions received by a committee, aggregated by the contributor’s employer name. If you are interested in our “is_individual” methodology, review the [methodology page](https://www.fec.gov/campaign-finance-data/about-campaign-finance-data/methodology). Unitemized individual contributions are not included.
 """,
    tags=[
        'committee_operations_management',
        'candidate_information_management',
        'filing_records_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_a_by_employer_(
    employer: Optional[Employer] = None,
    page: Optional[int] = 1,
    sort_nulls_last: Optional[bool] = False,
    cycle: Optional[Cycle] = None,
    sort_hide_null: Optional[bool] = False,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_a/by_occupation/',
    description=""" 
This endpoint provides itemized individual contributions received by a committee, aggregated by the contributor’s occupation. If you are interested in our “is_individual” methodology, review the [methodology page](https://www.fec.gov/campaign-finance-data/about-campaign-finance-data/methodology). Unitemized individual contributions are not included.
 """,
    tags=[
        'committee_operations_management',
        'filing_records_management',
        'candidate_information_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_a_by_occupation_(
    occupation: Optional[Occupation] = None,
    page: Optional[int] = 1,
    sort_nulls_last: Optional[bool] = False,
    cycle: Optional[Cycle] = None,
    sort_hide_null: Optional[bool] = False,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_a/by_size/',
    description=""" 
This endpoint provides individual contributions received by a committee, aggregated by size:

```
 - $200 and under
 - $200.01 - $499.99
 - $500 - $999.99
 - $1000 - $1999.99
 - $2000 +
```

The $200.00 and under category includes contributions of $200 or less combined with unitemized individual contributions.
 """,
    tags=[
        'filing_records_management',
        'committee_operations_management',
        'candidate_information_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_a_by_size_(
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    cycle: Optional[Cycle] = None,
    sort_hide_null: Optional[bool] = False,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    size: Optional[Size1] = None,
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_a/by_size/by_candidate/',
    description=""" 
This endpoint provides itemized individual contributions received by a committee, aggregated by size of contribution and candidate. If you are interested in our “is_individual” methodology, review the [methodology page](https://www.fec.gov/campaign-finance-data/about-campaign-finance-data/methodology). Unitemized individual contributions are not included.
 """,
    tags=[
        'candidate_information_management',
        'state_election_office_data_management',
        'candidate_financial_statistics',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_a_by_size_by_candidate_(
    candidate_id: CandidateId,
    sort_nulls_last: Optional[bool] = False,
    cycle: Cycle = ...,
    page: Optional[int] = 1,
    sort_hide_null: Optional[bool] = False,
    election_full: Optional[bool] = True,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_a/by_state/',
    description=""" 
This endpoint provides itemized individual contributions received by a committee, aggregated by the contributor’s state. If you are interested in our “is_individual” methodology, review the [methodology page](https://www.fec.gov/campaign-finance-data/about-campaign-finance-data/methodology). Unitemized individual contributions are not included.
 """,
    tags=[
        'financial_totals_summary',
        'filing_records_management',
        'committee_operations_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_a_by_state_(
    hide_null: Optional[bool] = False,
    page: Optional[int] = 1,
    cycle: Optional[Cycle] = None,
    state: Optional[State] = None,
    sort_hide_null: Optional[bool] = False,
    sort_nulls_last: Optional[bool] = False,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = '-total',
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_a/by_state/by_candidate/',
    description=""" 
This endpoint provides itemized individual contributions received by a committee, aggregated by contributor’s state and candidate. If you are interested in our “is_individual” methodology, review the [methodology page](https://www.fec.gov/campaign-finance-data/about-campaign-finance-data/methodology). Unitemized individual contributions are not included.
 """,
    tags=['candidate_information_management', 'financial_reporting_dates_management'],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_a_by_state_by_candidate_(
    candidate_id: CandidateId,
    sort_nulls_last: Optional[bool] = False,
    cycle: Cycle = ...,
    page: Optional[int] = 1,
    sort_hide_null: Optional[bool] = False,
    election_full: Optional[bool] = True,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_a/by_state/by_candidate/totals/',
    description=""" 
Itemized individual contributions aggregated by contributor’s state, candidate, committee type and cycle. If you are interested in our “is_individual” methodology, review the [methodology page](https://www.fec.gov/campaign-finance-data/about-campaign-finance-data/methodology). Unitemized individual contributions are not included.

 """,
    tags=[
        'candidate_information_management',
        'candidate_financial_statistics',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_a_by_state_by_candidate_totals_(
    candidate_id: CandidateId,
    sort_nulls_last: Optional[bool] = False,
    cycle: Cycle = ...,
    page: Optional[int] = 1,
    sort_hide_null: Optional[bool] = False,
    election_full: Optional[bool] = True,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_a/by_state/totals/',
    description=""" 
This endpoint provides itemized individual contributions received by a committee, aggregated by contributor’s state, committee type and cycle. If you are interested in our “is_individual” methodology, review the [methodology page](https://www.fec.gov/campaign-finance-data/about-campaign-finance-data/methodology). Unitemized individual contributions are not included.
 """,
    tags=['candidate_information_management', 'committee_operations_management'],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_a_by_state_totals_(
    committee_type: Optional[CommitteeType4] = None,
    cycle: Optional[Cycle] = None,
    page: Optional[int] = 1,
    state: Optional[State] = None,
    sort_hide_null: Optional[bool] = False,
    sort_nulls_last: Optional[bool] = False,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = 'cycle',
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_a/by_zip/',
    description=""" 
This endpoint provides itemized individual contributions received by a committee, aggregated by the contributor’s ZIP code. If you are interested in our “is_individual” methodology, review the [methodology page](https://www.fec.gov/campaign-finance-data/about-campaign-finance-data/methodology). Unitemized individual contributions are not included.
 """,
    tags=[
        'state_election_office_data_management',
        'filing_records_management',
        'committee_operations_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_a_by_zip_(
    page: Optional[int] = 1,
    cycle: Optional[Cycle] = None,
    zip: Optional[Zip1] = None,
    sort_hide_null: Optional[bool] = False,
    state: Optional[State] = None,
    committee_id: Optional[CommitteeId] = None,
    sort_nulls_last: Optional[bool] = False,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_a/efile/',
    description=""" 
Efiling endpoints provide real-time campaign finance data received from electronic filers. Efiling endpoints only contain the most recent four months of data and don't contain the processed and coded data that you can find on other endpoints.
 """,
    tags=[
        'candidate_financial_statistics',
        'committee_operations_management',
        'schedule_a_contribution_management',
        'filing_records_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_a_efile_(
    max_amount: Optional[str] = None,
    contributor_occupation: Optional[ContributorOccupation] = None,
    max_image_number: Optional[str] = None,
    sort_nulls_last: Optional[bool] = False,
    sort_hide_null: Optional[bool] = False,
    line_number: Optional[str] = None,
    per_page: Optional[int] = 20,
    contributor_city: Optional[ContributorCity] = None,
    contributor_state: Optional[ContributorState] = None,
    page: Optional[int] = 1,
    sort: Optional[str] = '-contribution_receipt_date',
    committee_id: Optional[CommitteeId] = None,
    min_date: Optional[date_aliased] = None,
    contributor_employer: Optional[ContributorEmployer] = None,
    api_key: str = 'DEMO_KEY',
    contributor_name: Optional[ContributorName] = None,
    min_image_number: Optional[str] = None,
    sort_null_only: Optional[bool] = False,
    image_number: Optional[ImageNumber] = None,
    min_amount: Optional[str] = None,
    max_date: Optional[date_aliased] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_a/{sub_id}/',
    description=""" 
This description is for both ​`/schedules​/schedule_a​/` and ​ `/schedules​/schedule_a​/{sub_id}​/`.

This endpoint provides itemized receipts. Schedule A records describe itemized receipts, including contributions from individuals. If you are interested in contributions from an individual, use the `/schedules/schedule_a/` endpoint. For a more complete description of all Schedule A records visit [About receipts data](https://www.fec.gov/campaign-finance-data/about-campaign-finance-data/about-receipts-data/). If you are interested in our "is_individual" methodology visit our [methodology page](https://www.fec.gov/campaign-finance-data/about-campaign-finance-data/methodology/).

​The `/schedules​/schedule_a​/` endpoint is not paginated by page number. This endpoint uses keyset pagination to improve query performance and these indices are required to properly page through this large dataset. To request the next page, you should append the values found in the `last_indexes` object from pagination to the URL of your last request as additional parameters. 
For example, when sorting by `contribution_receipt_date`, you might receive a page of results with the two scenarios of following pagination information:

case #1:
```
pagination: {
    pages: 2152643,
    per_page: 20,
    count: 43052850,
    last_indexes: {
        last_index: "230880619",
        last_contribution_receipt_date: "2014-01-01"
    }
}
```
<br/>
case #2 (results which include contribution_receipt_date = NULL):

```
pagination: {
    pages: 2152644,
    per_page: 20,
    count: 43052850,
    last_indexes: {
        last_index: "230880639",
        sort_null_only: True
    }
}
```
To fetch the next page of sorted results, append `last_index=230880619` and `last_contribution_receipt_date=2014-01-01` to the URL and when reaching `contribution_receipt_date=NULL`, append `last_index=230880639` and `sort_null_only=True`. We strongly advise paging through these results using sort indices. The default sort is acending by `contribution_receipt_date` (`deprecated`, will be descending). If you do not page using sort indices, some transactions may be unintentionally filtered out.

Calls to ​`/schedules​/schedule_a​/` may return many records. For large result sets, the record counts found in the pagination object are approximate; you will need to page through the records until no records are returned.

To avoid throwing the "out of range" exception on the last page, one recommandation is to use total count and `per_page` to control the traverse loop of results.

​The `/schedules​/schedule_a​/{sub_id}​/` endpoint returns a single transaction, but it does include a pagination object class. Please ignore the information in that object class.

 """,
    tags=[
        'schedule_a_contribution_management',
        'committee_operations_management',
        'candidate_financial_statistics',
        'transaction_operations_logging',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_a__sub_id_(
    max_load_date: Optional[date_aliased] = None,
    max_amount: Optional[str] = None,
    max_image_number: Optional[str] = None,
    contributor_occupation: Optional[ContributorOccupation] = None,
    recipient_committee_designation: Optional[RecipientCommitteeDesignation1] = None,
    sort_hide_null: Optional[bool] = False,
    is_individual: Optional[bool] = None,
    line_number: Optional[str] = None,
    contributor_type: Optional[ContributorType1] = None,
    recipient_committee_type: Optional[RecipientCommitteeType1] = None,
    per_page: Optional[int] = 20,
    contributor_zip: Optional[ContributorZip] = None,
    contributor_city: Optional[ContributorCity] = None,
    last_index: Optional[int] = None,
    last_contribution_receipt_amount: Optional[float] = None,
    contributor_id: Optional[ContributorId] = None,
    recipient_committee_org_type: Optional[RecipientCommitteeOrgType1] = None,
    contributor_state: Optional[ContributorState] = None,
    sort: Optional[str] = '-contribution_receipt_date',
    two_year_transaction_period: Optional[TwoYearTransactionPeriod] = None,
    min_date: Optional[date_aliased] = None,
    committee_id: Optional[CommitteeId] = None,
    contributor_employer: Optional[ContributorEmployer] = None,
    min_load_date: Optional[date_aliased] = None,
    api_key: str = 'DEMO_KEY',
    last_contribution_receipt_date: Optional[date_aliased] = None,
    min_image_number: Optional[str] = None,
    contributor_name: Optional[ContributorName] = None,
    sort_null_only: Optional[bool] = False,
    image_number: Optional[ImageNumber] = None,
    min_amount: Optional[str] = None,
    max_date: Optional[date_aliased] = None,
    sub_id: str = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_b/',
    description=""" 
Schedule B filings describe itemized disbursements. This data
explains how committees and other filers spend their money. These figures are
reported as part of forms F3, F3X and F3P.

The data is divided in two-year periods, called `two_year_transaction_period`, which
is derived from the `report_year` submitted of the corresponding form. If no value is supplied, the results will
default to the most recent two-year period that is named after the ending,
even-numbered year.

Due to the large quantity of Schedule B filings, this endpoint is not paginated by
page number. Instead, you can request the next page of results by adding the values in
the `last_indexes` object from `pagination` to the URL of your last request. For
example, when sorting by `disbursement_date`, you might receive a page of
results with the following pagination information:

```
pagination: {
    pages: 965191,
    per_page: 20,
    count: 19303814,
    last_indexes: {
        last_index: "230906248",
        last_disbursement_date: "2014-07-04"
    }
}
```

To fetch the next page of sorted results, append `last_index=230906248` and
`last_disbursement_date=2014-07-04` to the URL.  We strongly advise paging through
these results by using the sort indices (defaults to sort by disbursement date, e.g.
`last_disbursement_date`), otherwise some resources may be unintentionally filtered out.
This resource uses keyset pagination to improve query performance
and these indices are required to properly page through this large dataset.

Note: because the Schedule B data includes many records, counts for
large result sets are approximate; you will want to page through the records until no records are returned.
 """,
    tags=[
        'schedule_b_disbursement_management',
        'financial_totals_summary',
        'transaction_operations_logging',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_b_(
    disbursement_description: Optional[DisbursementDescription] = None,
    max_amount: Optional[str] = None,
    max_image_number: Optional[str] = None,
    image_number: Optional[ImageNumber] = None,
    sort_hide_null: Optional[bool] = False,
    last_disbursement_date: Optional[date_aliased] = None,
    line_number: Optional[str] = None,
    spender_committee_designation: Optional[SpenderCommitteeDesignation] = None,
    last_disbursement_amount: Optional[float] = None,
    per_page: Optional[int] = 20,
    last_index: Optional[int] = None,
    spender_committee_type: Optional[SpenderCommitteeType] = None,
    sort: Optional[str] = '-disbursement_date',
    two_year_transaction_period: Optional[TwoYearTransactionPeriod] = None,
    min_date: Optional[date_aliased] = None,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    disbursement_purpose_category: Optional[DisbursementPurposeCategory] = None,
    recipient_name: Optional[RecipientName] = None,
    recipient_state: Optional[RecipientState] = None,
    recipient_city: Optional[RecipientCity] = None,
    min_image_number: Optional[str] = None,
    spender_committee_org_type: Optional[SpenderCommitteeOrgType] = None,
    sort_null_only: Optional[bool] = False,
    recipient_committee_id: Optional[RecipientCommitteeId] = None,
    min_amount: Optional[str] = None,
    max_date: Optional[date_aliased] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_b/by_purpose/',
    description=""" 
Schedule B disbursements aggregated by disbursement purpose category. To avoid double counting,
memoed items are not included.
Purpose is a combination of transaction codes, category codes and disbursement description.
Inspect the `disbursement_purpose` sql function within the migrations for more details.
 """,
    tags=[
        'candidate_information_management',
        'committee_operations_management',
        'filing_records_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_b_by_purpose_(
    purpose: Optional[Purpose] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    cycle: Optional[Cycle] = None,
    sort_hide_null: Optional[bool] = False,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_b/by_recipient/',
    description=""" 
Schedule B disbursements aggregated by recipient name. To avoid double counting,
memoed items are not included.
 """,
    tags=[
        'committee_operations_management',
        'filing_records_management',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_b_by_recipient_(
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    cycle: Optional[Cycle] = None,
    sort_hide_null: Optional[bool] = False,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    recipient_name: Optional[RecipientName] = None,
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_b/by_recipient_id/',
    description=""" 
Schedule B disbursements aggregated by recipient committee ID, if applicable.
To avoid double counting, memoed items are not included.
 """,
    tags=['filing_records_management', 'committee_operations_management'],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_b_by_recipient_id_(
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    cycle: Optional[Cycle] = None,
    sort_hide_null: Optional[bool] = False,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    recipient_id: Optional[RecipientId] = None,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_b/efile/',
    description=""" 
Efiling endpoints provide real-time campaign finance data received from electronic filers. Efiling endpoints only contain the most recent four months of data and don't contain the processed and coded data that you can find on other endpoints.
 """,
    tags=[
        'schedule_b_disbursement_management',
        'financial_totals_summary',
        'filing_records_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_b_efile_(
    disbursement_description: Optional[DisbursementDescription] = None,
    max_amount: Optional[str] = None,
    page: Optional[int] = 1,
    sort_nulls_last: Optional[bool] = False,
    sort: Optional[str] = '-disbursement_date',
    sort_hide_null: Optional[bool] = False,
    committee_id: Optional[CommitteeId] = None,
    min_date: Optional[date_aliased] = None,
    api_key: str = 'DEMO_KEY',
    recipient_state: Optional[RecipientState] = None,
    recipient_city: Optional[RecipientCity] = None,
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    image_number: Optional[ImageNumber] = None,
    min_amount: Optional[str] = None,
    max_date: Optional[date_aliased] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_b/{sub_id}/',
    description=""" 
Schedule B filings describe itemized disbursements. This data
explains how committees and other filers spend their money. These figures are
reported as part of forms F3, F3X and F3P.

The data is divided in two-year periods, called `two_year_transaction_period`, which
is derived from the `report_year` submitted of the corresponding form. If no value is supplied, the results will
default to the most recent two-year period that is named after the ending,
even-numbered year.

Due to the large quantity of Schedule B filings, this endpoint is not paginated by
page number. Instead, you can request the next page of results by adding the values in
the `last_indexes` object from `pagination` to the URL of your last request. For
example, when sorting by `disbursement_date`, you might receive a page of
results with the following pagination information:

```
pagination: {
    pages: 965191,
    per_page: 20,
    count: 19303814,
    last_indexes: {
        last_index: "230906248",
        last_disbursement_date: "2014-07-04"
    }
}
```

To fetch the next page of sorted results, append `last_index=230906248` and
`last_disbursement_date=2014-07-04` to the URL.  We strongly advise paging through
these results by using the sort indices (defaults to sort by disbursement date, e.g.
`last_disbursement_date`), otherwise some resources may be unintentionally filtered out.
This resource uses keyset pagination to improve query performance
and these indices are required to properly page through this large dataset.

Note: because the Schedule B data includes many records, counts for
large result sets are approximate; you will want to page through the records until no records are returned.
 """,
    tags=[
        'schedule_b_disbursement_management',
        'committee_operations_management',
        'financial_metrics_retrieval',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_b__sub_id_(
    disbursement_description: Optional[DisbursementDescription] = None,
    max_amount: Optional[str] = None,
    max_image_number: Optional[str] = None,
    image_number: Optional[ImageNumber] = None,
    sort_hide_null: Optional[bool] = False,
    last_disbursement_date: Optional[date_aliased] = None,
    line_number: Optional[str] = None,
    spender_committee_designation: Optional[SpenderCommitteeDesignation1] = None,
    last_disbursement_amount: Optional[float] = None,
    per_page: Optional[int] = 20,
    last_index: Optional[int] = None,
    spender_committee_type: Optional[SpenderCommitteeType1] = None,
    sort: Optional[str] = '-disbursement_date',
    two_year_transaction_period: Optional[TwoYearTransactionPeriod] = None,
    min_date: Optional[date_aliased] = None,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    disbursement_purpose_category: Optional[DisbursementPurposeCategory] = None,
    recipient_name: Optional[RecipientName] = None,
    recipient_state: Optional[RecipientState] = None,
    recipient_city: Optional[RecipientCity] = None,
    min_image_number: Optional[str] = None,
    spender_committee_org_type: Optional[SpenderCommitteeOrgType1] = None,
    sort_null_only: Optional[bool] = False,
    recipient_committee_id: Optional[RecipientCommitteeId] = None,
    min_amount: Optional[str] = None,
    max_date: Optional[date_aliased] = None,
    sub_id: str = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_c/',
    description=""" 
Schedule C shows all loans, endorsements and loan guarantees a committee
receives or makes.

The committee continues to report the loan until it is repaid.
 """,
    tags=[
        'candidate_information_management',
        'committee_operations_management',
        'filing_records_management',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_c_(
    min_incurred_date: Optional[date_aliased] = None,
    candidate_name: Optional[CandidateName] = None,
    max_amount: Optional[str] = None,
    max_image_number: Optional[str] = None,
    sort_nulls_last: Optional[bool] = True,
    sort_hide_null: Optional[bool] = False,
    line_number: Optional[str] = None,
    max_incurred_date: Optional[date_aliased] = None,
    per_page: Optional[int] = 20,
    last_index: Optional[int] = None,
    loan_source_name: Optional[LoanSourceName] = None,
    page: Optional[int] = 1,
    sort: Optional[str] = '-incurred_date',
    max_payment_to_date: Optional[int] = None,
    min_payment_to_date: Optional[int] = None,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    min_image_number: Optional[str] = None,
    sort_null_only: Optional[bool] = False,
    image_number: Optional[ImageNumber] = None,
    min_amount: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_c/{sub_id}/',
    description=""" 
Schedule C shows all loans, endorsements and loan guarantees a committee
receives or makes.

The committee continues to report the loan until it is repaid.
 """,
    tags=[
        'filing_records_management',
        'financial_totals_summary',
        'financial_metrics_retrieval',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_c__sub_id_(
    sort_nulls_last: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort_null_only: Optional[bool] = False,
    sort_hide_null: Optional[bool] = False,
    sort: Optional[str] = None,
    api_key: str = 'DEMO_KEY',
    page: Optional[int] = 1,
    sub_id: str = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_d/',
    description=""" 
Schedule D, it shows debts and obligations owed to or by the committee that are
required to be disclosed.


 """,
    tags=['schedule_c_debt_management', 'schedule_d_outstanding_debt_management'],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_d_(
    creditor_debtor_name: Optional[CreditorDebtorName] = None,
    max_image_number: Optional[str] = None,
    sort_nulls_last: Optional[bool] = False,
    max_amount_outstanding_beginning: Optional[float] = None,
    sort_hide_null: Optional[bool] = False,
    min_payment_period: Optional[float] = None,
    max_amount_incurred: Optional[float] = None,
    nature_of_debt: Optional[str] = None,
    per_page: Optional[int] = 20,
    max_amount_outstanding_close: Optional[float] = None,
    candidate_id: Optional[CandidateId] = None,
    page: Optional[int] = 1,
    min_date: Optional[date_aliased] = None,
    committee_id: Optional[CommitteeId] = None,
    min_amount_outstanding_close: Optional[float] = None,
    api_key: str = 'DEMO_KEY',
    max_payment_period: Optional[float] = None,
    min_image_number: Optional[str] = None,
    min_amount_incurred: Optional[float] = None,
    sort_null_only: Optional[bool] = False,
    image_number: Optional[ImageNumber] = None,
    sort: Optional[str] = 'load_date',
    min_amount_outstanding_beginning: Optional[float] = None,
    max_date: Optional[date_aliased] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_d/{sub_id}/',
    description=""" 
Schedule D, it shows debts and obligations owed to or by the committee that are
required to be disclosed.


 """,
    tags=[
        'financial_metrics_retrieval',
        'filing_records_management',
        'state_election_office_data_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_d__sub_id_(
    sort_nulls_last: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort_null_only: Optional[bool] = False,
    sort_hide_null: Optional[bool] = False,
    sort: Optional[str] = 'load_date',
    api_key: str = 'DEMO_KEY',
    page: Optional[int] = 1,
    sub_id: str = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_e/',
    description=""" 
Schedule E covers the line item expenditures for independent expenditures. For example, if a super PAC
bought ads on TV to oppose a federal candidate, each ad purchase would be recorded here with
the expenditure amount, name and id of the candidate, and whether the ad supported or opposed the candidate.

An independent expenditure is an expenditure for a communication "expressly advocating the election or
defeat of a clearly identified candidate that is not made in cooperation, consultation, or concert with,
or at the request or suggestion of, a candidate, a candidate’s authorized committee, or their agents, or
a political party or its agents."

Aggregates by candidate do not include 24 and 48 hour reports. This ensures we don't double count expenditures
and the totals are more accurate. You can still find the information from 24 and 48 hour reports in
`/schedule/schedule_e/`.

Due to the large quantity of Schedule E filings, this endpoint is not paginated by
page number. Instead, you can request the next page of results by adding the values in
the `last_indexes` object from `pagination` to the URL of your last request. For
example, when sorting by `expenditure_amount`, you might receive a page of
results with the following pagination information:

```
 "pagination": {
    "count": 152623,
    "last_indexes": {
      "last_index": "3023037",
      "last_expenditure_amount": -17348.5
    },
    "per_page": 20,
    "pages": 7632
  }
}
```

To fetch the next page of sorted results, append `last_index=3023037` and
`last_expenditure_amount=` to the URL.  We strongly advise paging through
these results by using the sort indices (defaults to sort by disbursement date,
e.g. `last_disbursement_date`), otherwise some resources may be unintentionally
filtered out.  This resource uses keyset pagination to improve query performance
and these indices are required to properly page through this large dataset.

Note: because the Schedule E data includes many records, counts for
large result sets are approximate; you will want to page through the records until no records are returned.
 """,
    tags=[
        'candidate_financial_statistics',
        'filing_records_management',
        'schedule_e_expenditure_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_e_(
    max_dissemination_date: Optional[date_aliased] = None,
    sort_hide_null: Optional[bool] = False,
    payee_name: Optional[PayeeName] = None,
    q_spender: Optional[QSpender] = None,
    per_page: Optional[int] = 20,
    last_index: Optional[int] = None,
    min_dissemination_date: Optional[date_aliased] = None,
    candidate_office_state: Optional[CandidateOfficeState] = None,
    api_key: str = 'DEMO_KEY',
    filing_form: Optional[FilingForm] = None,
    sort_null_only: Optional[bool] = False,
    max_amount: Optional[str] = None,
    max_image_number: Optional[str] = None,
    min_filing_date: Optional[date_aliased] = None,
    cycle: Optional[Cycle] = None,
    sort_nulls_last: Optional[bool] = False,
    support_oppose_indicator: Optional[SupportOpposeIndicator3] = None,
    candidate_office: Optional[CandidateOffice] = None,
    is_notice: Optional[IsNotice] = None,
    line_number: Optional[str] = None,
    last_expenditure_amount: Optional[float] = None,
    last_expenditure_date: Optional[date_aliased] = None,
    candidate_id: Optional[CandidateId] = None,
    last_office_total_ytd: Optional[float] = None,
    last_support_oppose_indicator: Optional[str] = None,
    min_date: Optional[date_aliased] = None,
    candidate_party: Optional[CandidateParty] = None,
    committee_id: Optional[CommitteeId] = None,
    min_image_number: Optional[str] = None,
    max_filing_date: Optional[date_aliased] = None,
    candidate_office_district: Optional[CandidateOfficeDistrict] = None,
    min_amount: Optional[str] = None,
    image_number: Optional[ImageNumber] = None,
    sort: Optional[str] = '-expenditure_date',
    max_date: Optional[date_aliased] = None,
    most_recent: Optional[bool] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_e/by_candidate/',
    description=""" 
Schedule E receipts aggregated by recipient candidate. To avoid double
counting, memoed items are not included.
 """,
    tags=[
        'candidate_information_management',
        'committee_operations_management',
        'election_information_retrieval',
        'filing_records_management',
        'state_election_office_data_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_e_by_candidate_(
    office: Optional[Office23] = None,
    candidate_id: Optional[CandidateId] = None,
    cycle: Optional[Cycle] = None,
    district: Optional[str] = None,
    state: Optional[str] = None,
    page: Optional[int] = 1,
    support_oppose: Optional[SupportOppose] = None,
    election_full: Optional[bool] = True,
    committee_id: Optional[CommitteeId] = None,
    sort_hide_null: Optional[bool] = False,
    sort_nulls_last: Optional[bool] = False,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_e/efile/',
    description=""" 
Efiling endpoints provide real-time campaign finance data received from electronic filers. Efiling endpoints only contain the most recent four months of data and don't contain the processed and coded data that you can find on other endpoints.
 """,
    tags=[
        'candidate_financial_statistics',
        'electioneering_expense_data',
        'schedule_e_expenditure_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_e_efile_(
    spender_name: Optional[SpenderName] = None,
    min_expenditure_date: Optional[date_aliased] = None,
    sort_nulls_last: Optional[bool] = False,
    max_dissemination_date: Optional[date_aliased] = None,
    support_oppose_indicator: Optional[SupportOpposeIndicator4] = None,
    sort_hide_null: Optional[bool] = False,
    candidate_office: Optional[CandidateOffice1] = None,
    is_notice: Optional[bool] = None,
    payee_name: Optional[PayeeName] = None,
    per_page: Optional[int] = 20,
    candidate_search: Optional[CandidateSearch1] = None,
    max_expenditure_amount: Optional[int] = None,
    min_dissemination_date: Optional[date_aliased] = None,
    min_filed_date: Optional[date_aliased] = None,
    candidate_id: Optional[CandidateId] = None,
    candidate_office_state: Optional[CandidateOfficeState] = None,
    max_filed_date: Optional[date_aliased] = None,
    page: Optional[int] = 1,
    min_expenditure_amount: Optional[int] = None,
    committee_id: Optional[CommitteeId] = None,
    candidate_party: Optional[CandidateParty] = None,
    api_key: str = 'DEMO_KEY',
    max_expenditure_date: Optional[date_aliased] = None,
    filing_form: Optional[FilingForm] = None,
    sort_null_only: Optional[bool] = False,
    candidate_office_district: Optional[CandidateOfficeDistrict] = None,
    image_number: Optional[ImageNumber] = None,
    sort: Optional[str] = '-expenditure_date',
    most_recent: Optional[bool] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_e/totals/by_candidate/',
    description=""" 
Total independent expenditure on supported or opposed candidates by cycle or candidate election year.
 """,
    tags=[
        'candidate_information_management',
        'election_information_retrieval',
        'filing_records_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_e_totals_by_candidate_(
    candidate_id: Optional[CandidateId] = None,
    sort_nulls_last: Optional[bool] = False,
    page: Optional[int] = 1,
    cycle: Optional[Cycle] = None,
    sort_hide_null: Optional[bool] = False,
    election_full: Optional[bool] = True,
    api_key: str = 'DEMO_KEY',
    sort_null_only: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort: Optional[str] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_f/',
    description=""" 
Schedule F, it shows all special expenditures a national or state party committee
makes in connection with the general election campaigns of federal candidates.

These coordinated party expenditures do not count against the contribution limits but are subject to other limits,
these limits are detailed in Chapter 7 of the FEC Campaign Guide for Political Party Committees.
 """,
    tags=[
        'candidate_information_management',
        'committee_operations_management',
        'filing_records_management',
        'schedule_e_expenditure_management',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_f_(
    max_amount: Optional[str] = None,
    max_image_number: Optional[str] = None,
    cycle: Optional[Cycle] = None,
    sort_nulls_last: Optional[bool] = False,
    sort_hide_null: Optional[bool] = False,
    line_number: Optional[str] = None,
    payee_name: Optional[PayeeName] = None,
    per_page: Optional[int] = 20,
    candidate_id: Optional[CandidateId] = None,
    page: Optional[int] = 1,
    min_date: Optional[date_aliased] = None,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    min_amount: Optional[str] = None,
    min_image_number: Optional[str] = None,
    sort_null_only: Optional[bool] = False,
    image_number: Optional[ImageNumber] = None,
    sort: Optional[str] = 'expenditure_date',
    max_date: Optional[date_aliased] = None,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/schedules/schedule_f/{sub_id}/',
    description=""" 
Schedule F, it shows all special expenditures a national or state party committee
makes in connection with the general election campaigns of federal candidates.

These coordinated party expenditures do not count against the contribution limits but are subject to other limits,
these limits are detailed in Chapter 7 of the FEC Campaign Guide for Political Party Committees.
 """,
    tags=['state_election_office_data_management', 'financial_totals_summary'],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_schedules_schedule_f__sub_id_(
    per_page: Optional[int] = 20,
    api_key: str = 'DEMO_KEY',
    page: Optional[int] = 1,
    sub_id: str = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/state-election-office/',
    description=""" 
State laws and procedures govern elections for state or local offices as well as
how candidates appear on election ballots.
Contact the appropriate state election office for more information.
 """,
    tags=[
        'filing_records_management',
        'state_election_office_data_management',
        'financial_totals_summary',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_state_election_office_(
    sort_null_only: Optional[bool] = False,
    sort_nulls_last: Optional[bool] = False,
    per_page: Optional[int] = 20,
    state: str = ...,
    sort_hide_null: Optional[bool] = False,
    sort: Optional[str] = None,
    api_key: str = 'DEMO_KEY',
    page: Optional[int] = 1,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/totals/by_entity/',
    description=""" 
Provides cumulative receipt totals by entity type, over a two year cycle. Totals are adjusted to avoid double counting.

This is [the sql](https://github.com/fecgov/openFEC/blob/develop/data/migrations/V41__large_aggregates.sql) that creates these calculations.
 """,
    tags=['state_election_office_data_management', 'filing_records_management'],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_totals_by_entity_(
    cycle: int,
    sort_nulls_last: Optional[bool] = False,
    per_page: Optional[int] = 20,
    sort_null_only: Optional[bool] = False,
    sort_hide_null: Optional[bool] = False,
    sort: Optional[str] = 'end_date',
    api_key: str = 'DEMO_KEY',
    page: Optional[int] = 1,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


@app.get(
    '/totals/{entity_type}/',
    description=""" 
This endpoint provides information about a committee's Form 3, Form 3X, or Form 3P financial reports,
which are aggregated by two-year period. We refer to two-year periods as a `cycle`.

The cycle is named after the even-numbered year and includes the year before it. To obtain
totals from 2013 and 2014, you would use 2014. In odd-numbered years, the current cycle
is the next year — for example, in 2015, the current cycle is 2016.

For presidential and Senate candidates, multiple two-year cycles exist between elections.

 """,
    tags=[
        'committee_operations_management',
        'filing_records_management',
        'financial_reporting_dates_management',
    ],
    security=[
        APIKeyHeader(name="X-Api-Key"),
        APIKeyQuery(name="api_key"),
        APIKeyQuery(name="api_key"),
    ],
)
def get_totals__entity_type_(
    max_first_f1_date: Optional[date_aliased] = None,
    min_receipts: Optional[str] = None,
    cycle: Optional[Cycle] = None,
    filing_frequency: Optional[FilingFrequency3] = None,
    max_receipts: Optional[str] = None,
    sort_hide_null: Optional[bool] = False,
    sort_nulls_last: Optional[bool] = False,
    min_last_debts_owed_by_committee: Optional[str] = None,
    max_last_cash_on_hand_end_period: Optional[str] = None,
    treasurer_name: Optional[TreasurerName] = None,
    sponsor_candidate_id: Optional[SponsorCandidateId] = None,
    per_page: Optional[int] = 20,
    max_disbursements: Optional[str] = None,
    committee_state: Optional[CommitteeState] = None,
    committee_type: Optional[CommitteeType4] = None,
    page: Optional[int] = 1,
    max_last_debts_owed_by_committee: Optional[str] = None,
    committee_id: Optional[CommitteeId] = None,
    api_key: str = 'DEMO_KEY',
    committee_designation: Optional[CommitteeDesignation] = None,
    sort_null_only: Optional[bool] = False,
    min_first_f1_date: Optional[date_aliased] = None,
    organization_type: Optional[OrganizationType3] = None,
    sort: Optional[str] = '-cycle',
    min_disbursements: Optional[str] = None,
    min_last_cash_on_hand_end_period: Optional[str] = None,
    entity_type: EntityType1 = ...,
):
    raise RuntimeError("Should be patched by MCPProxy and never executed")


if __name__ == "__main__":
    parser = argparse.ArgumentParser(description="MCP Server")
    parser.add_argument(
        "transport",
        choices=["stdio", "sse", "streamable-http"],
        help="Transport mode (stdio, sse or streamable-http)",
    )
    args = parser.parse_args()

    if "CONFIG_PATH" in os.environ:
        config_path = os.environ["CONFIG_PATH"]
        app.load_configuration(config_path)

    if "CONFIG" in os.environ:
        config = os.environ["CONFIG"]
        app.load_configuration_from_string(config)

    if "SECURITY" in os.environ:
        security_params = BaseSecurity.parse_security_parameters_from_env(
            os.environ,
        )

        app.set_security_params(security_params)

    mcp_settings = json.loads(os.environ.get("MCP_SETTINGS", "{}"))

    app.get_mcp(**mcp_settings).run(transport=args.transport)
